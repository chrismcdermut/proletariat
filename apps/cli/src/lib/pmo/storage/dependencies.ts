/**
 * Dependency operations for tickets, specs, and epics.
 *
 * PRLT-1299: This module has been removed. Dependency tables have been dropped.
 * The ticket provider owns dependency relationships.
 */

// Empty export to prevent import errors during migration
export {}
