/**
 * Workflow status operations.
 *
 * PRLT-1299: This module has been removed. Workflow and workflow status tables
 * have been dropped. The ticket provider owns workflow state definitions.
 */

// Empty export to prevent import errors during migration
export {}
