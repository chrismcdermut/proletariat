# Proletariat Roadmap

## Project 1: MVP Launch

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| - | Local dev testing workflow (pnpm scripts + PRLT_HOME) | backlog | P0 | devx | **Problem**: When dogfooding prlt to build prlt with multiple agent worktrees on different feature branches, testing is difficult. Global `npm link` installs overwrite each other, and all agents share the same `.proletariat/workspace.db` causing conflicts. Need CLI equivalent of port isolation for web apps. **Solution**: (1) **Add pnpm scripts** to root `package.json` for local testing: `"prlt": "node apps/cli/bin/run.js"` (quick test with shared DB), `"prlt:test": "PRLT_HOME=/tmp/prlt-test-$$ node apps/cli/bin/run.js"` (isolated test with temp DB). Works from any subdirectory in workspace (pnpm finds root). (2) **Add PRLT_HOME env var support**: Allow overriding `.proletariat` directory location via `PRLT_HOME` environment variable. Update config resolution in storage-sqlite.ts or wherever `.proletariat` path is determined to check: `process.env.PRLT_HOME || path.join(cwd, '.proletariat')`. (3) **Document workflow** in CONTRIBUTING.md or developer docs: how to test changes from agent worktrees without global installs or database conflicts. **Usage examples**: `cd agents/alice && pnpm prlt ticket list` (test alice's build), `cd agents/bob && pnpm prlt:test init test-workspace` (isolated test for bob's build), `PRLT_HOME=/tmp/test-db pnpm prlt ticket create` (manual isolation). **Benefits**: (a) No more global install conflicts between agent builds. (b) Each agent can have isolated test database. (c) Natural muscle memory: `pnpm prlt <command>`. (d) Matches how other CLI tools handle local dev (Next.js, Vite, etc.). (e) Works from anywhere in workspace. **Priority P0**: Critical for productive dogfooding workflow - currently testing multiple agent branches is painful. This is internal DX, not end-user feature, but blocks efficient development. |
| - | Workspace discovery creates multiple isolated PMOs | backlog | P0 | bug | **Problem**: Running `prlt` from different directories creates separate `.proletariat/workspace.db` databases, each with isolated tickets, boards, agents, etc. User sees different data depending on which directory they run commands from. **Expected behavior**: Single source of truth for PMO data. Running `prlt ticket list` from any subdirectory should show the same tickets. **Notes**: [2025-01-07] Investigation findings - Found multiple `.proletariat` directories in proletariat-hq: (1) `/proletariat-hq/.proletariat` - Main HQ with valid `config.json` (`type: 'hq'`) ✅. (2) `/proletariat-hq/repos/proletariat/.proletariat` - Only contains `scripts/` folder, no workspace.db - NOT a PMO, just coincidental directory name (devcontainer scripts). (3) `/agents/staff/altman/proletariat-altman/.proletariat` - Has 0-byte `workspace.db` (empty file), no `config.json` - failed/partial init, orphaned. (4) `/agents/staff/altman/proletariat-altman/apps/cli/.pmo/.proletariat` - Standalone mini-PMO created by `prlt pmo init` with its own `config.json` (`type: 'hq'`). Actual PMO workspaces: Only #1 and #4 are real PMOs. The `findPMO()` logic correctly walks up looking for `.proletariat/config.json` with `type: 'hq'`, but #4 was created when agent ran `prlt pmo init` using a locally-built version (possibly with bugs or different HQ path context). Root cause: `prlt pmo init` creates standalone mini-HQ when no HQ is found in parent tree, but doesn't warn user if HQ exists higher up. Agents running local builds may have different discovery behavior. **Solution - Multi-part fix**: (1) **Add confirmation before standalone PMO**: When `pmo init` is about to create standalone `.pmo/`, check if HQ exists higher in tree and prompt "HQ found at /path/to/hq. Create PMO there instead? [Y/n]". (2) **PRLT_HOME env var** (related ticket): Explicit override for workspace location. (3) **Warn on multiple workspaces**: On any `prlt` command, detect if multiple `.proletariat/` dirs with `type: 'hq'` exist in parent tree and warn user. (4) **`prlt workspace` commands**: `prlt workspace list` (show all discovered workspaces), `prlt workspace use <path>` (set active workspace), `prlt workspace merge <source> <target>` (consolidate databases). (5) **Consolidation command**: `prlt workspace consolidate` or `prlt pmo merge <source-db> <target-db>` to merge tickets, specs, projects from orphan PMOs into main HQ database. Should handle: duplicate ticket IDs (renumber), conflicting project names (prompt or auto-suffix), preserve all history. **Implementation for consolidation**: (1) `prlt workspace list` - Scan current directory tree for all `.proletariat` dirs, show which have valid PMO tables, which are orphaned. (2) `prlt workspace merge <source> [--into <target>]` - Read all tickets/specs/projects from source DB, insert into target DB with new IDs if conflicts, delete source after confirmation. SQL: `INSERT INTO pmo_tickets SELECT ... FROM source.pmo_tickets` with ID remapping. (3) Handle foreign keys: ticket_specs, subtasks, assignments must be remapped to new ticket IDs. **Cleanup tasks for proletariat-hq**: (a) Delete orphan empty file: `rm /agents/staff/altman/proletariat-altman/.proletariat/workspace.db`. (b) Migrate tickets from standalone PMO to main HQ using merge command, then delete `/apps/cli/.pmo/`. **Related tickets**: Local dev testing workflow (PRLT_HOME), Flexible workspace init. **Priority P0**: Actively causing confusion during dogfooding. Users lose track of their tickets/boards because they're scattered across multiple databases. Blocks productive use of prlt. |
| - | Documentation and README | backlog | P0 | docs | Getting started guide, feature overview, examples. Can't adopt what you can't understand. Critical for beta launch. |
| - | Installation comparison guide (npm vs Homebrew vs binary) | backlog | P1 | docs | Create comprehensive guide explaining different installation methods and when to use each. **Problem**: Users unfamiliar with CLI distribution will be confused by multiple install options (npm, brew, apt, binary download). Need clear guidance on which method to choose. **Content to cover**: (1) **Quick comparison table**: | Method | Best For | Pros | Cons | - npm: JS developers, Already have Node.js, Familiar to JS devs, Requires Node.js - brew: macOS/Linux users, Automatic updates, Most popular, macOS/Linux only - apt/yum: Linux sysadmins, System integration, Enterprise-friendly, Linux only - choco: Windows users, Windows package manager, Easy updates, Windows only - Binary: Enterprise/offline, No dependencies, Works anywhere, Manual updates. (2) **Detailed explanations**: (a) **npm (Node Package Manager)**: Requires Node.js 18+ installed. Best if you already use Node.js for development. Global install can conflict with other tools. Example: `npm install -g @proletariat/cli`. (b) **Homebrew (macOS/Linux)**: System package manager, handles dependencies automatically. Most popular on macOS (70% of devs use it). Updates via `brew upgrade`. Example: `brew install proletariat`. (c) **Native packages (APT/YUM/Chocolatey)**: Integrates with system package manager. IT departments prefer this for security/compliance. Auto-updates through system. Examples: `apt install proletariat`, `choco install proletariat`. (d) **Pre-built binaries**: Zero dependencies, bundles Node.js runtime. Best for enterprise with restricted package access or offline environments. Manual updates via `prlt upgrade`. Example: `curl -fsSL https://... | sh`. (3) **Decision tree/flowchart**: "Which install method should I use?" - Already have Node.js? → npm - macOS user? → Homebrew - Linux sysadmin? → APT/YUM - Windows user? → Chocolatey - Enterprise/offline? → Binary download. (4) **Common questions**: Q: "What's the difference between npm and Homebrew?" A: npm is JavaScript-specific, requires Node.js. Homebrew is system-wide, works for any software. Q: "Can I use multiple install methods?" A: Not recommended - stick to one to avoid conflicts. Q: "Which is fastest?" A: Binary download (no dependencies), but Homebrew has best UX. Q: "Which auto-updates?" A: Homebrew, APT, Chocolatey auto-update. npm and binary require manual `upgrade`. (5) **Troubleshooting**: Common issues with each method (PATH conflicts, permission errors, version mismatches) and how to fix. **Location**: Add section to main README.md under "Installation", create detailed guide in docs/installation.md. Link from landing page. **Why P1 priority**: Confusion at install step = lost users. Clear guidance reduces friction. Many developers unfamiliar with the npm vs Homebrew distinction - especially backend/DevOps engineers who don't use JavaScript but are target audience. **Related to**: Homebrew distribution (line 165), package distribution (line 166), GitHub releases (line 167), documentation (line 8). |
| - | Flexible workspace init (remove HQ folder convention) | backlog | P0 | feature | Currently requires `-hq` suffix and specific folder structure. Should support: `prlt init` in any directory, no naming conventions required. Global config (~/.proletariat/config.json) can track multiple workspaces. Support single repo, monorepo, multi-repo setups. Reduces friction for adoption. |
| TKT-046 | Docker execution safety | backlog | P0 | fix | Check Docker availability before operations, fail fast with helpful message. `work spawn` silently falls back to host when Docker unavailable - should prompt for confirmation or require `--run-on-host` flag. Security concern. |
| - | Landing page | backlog | P1 | docs | Simple landing page. What is prlt, why use it, quick start, link to docs/GitHub. Can be GitHub Pages or dedicated site. First touchpoint for discovery. |
| - | Demo video | backlog | P1 | docs | Short video showing prlt in action - spawn agents, work on tickets, create PRs. Visual proof it works. |
| - | Graceful Ctrl+C exit | backlog | P1 | fix | Smooth shutdown, no stack traces. Clean exit handling. First impressions matter. |
| - | Feedback mechanism | backlog | P1 | feature | `prlt feedback` command to create GitHub issues directly from CLI. Opens issue creation page in browser with pre-filled template including system context (OS, version, last command). Phase 1: browser-based (no auth needed). Phase 2: API integration for in-terminal issue creation. Make it easy for users to report issues without leaving the terminal. |
| - | CLI-native support chat and help system | backlog | P2 | feature | In-terminal AI-powered support chat for instant help - "Intercom for CLI". **Vision**: First CLI tool with built-in real-time support. Users get instant answers without leaving terminal, you learn what users struggle with, builds unique differentiation. **Commands**: (1) `prlt chat <question>` or `prlt help chat` - Opens terminal chat interface for interactive help. Example: `prlt chat "How do I assign a ticket to an agent?"`. (2) `prlt help <command> --interactive` - Interactive contextual help with examples and Q&A. Shows command docs then offers "Ask a question" prompt. (3) `prlt support list` (admin) - View pending support questions from users. (4) `prlt support answer <id> <response>` (admin) - Respond to user questions. (5) `prlt announce <message>` (admin) - Push announcement shown to users on next command. (6) `prlt subscribe` - Collect email for feature announcements and tips. **Architecture**: (1) **Terminal UI**: Use `ink` or similar for chat interface. Multi-turn conversation in terminal. (2) **AI auto-response layer**: Claude API answers common questions instantly by searching docs/examples. Trained on prlt documentation, GitHub issues, common patterns. (3) **Human fallback**: Complex/unique questions routed to you via simple API/database. You see question + user context (OS, version, command history). (4) **Message queue**: Simple API endpoint stores questions in queue. WebSocket or polling for real-time updates. (5) **Context capture**: Include user's current workspace state, recent commands, error messages for better support. **Auto-response capabilities**: (1) Command syntax help - "How do I create a ticket?" → Shows `prlt ticket create` with examples. (2) Error troubleshooting - "Docker not found error" → Checks common causes, suggests fixes. (3) Workflow guidance - "How do I get started?" → Interactive tutorial walkthrough. (4) Concept explanation - "What's an agent?" → Explains with examples from user's workspace. (5) Fallback to human - "I found a bug" or complex questions → Routes to you with context. **Announcement system**: Push announcements shown as banner on next command: "🚀 New feature: GitHub sync is now available! Run `prlt github connect` to try it." Users can dismiss or click through for details. **Data collection benefits**: (1) **Product insights**: See what users struggle with most (improves docs, UX). (2) **Training data**: Build dataset of (question, answer, outcome) for fine-tuning. (3) **Bug discovery**: Users report issues directly in context. (4) **Feature validation**: "Can prlt do X?" questions reveal feature gaps. (5) **User engagement**: Personal touch differentiates from competitors. **Privacy & opt-in**: (1) User opts in during `prlt init` or first `prlt chat` use. (2) No PII sent - only anonymized user ID + technical context. (3) Chat history stored locally, only questions sent to server. (4) Clear privacy policy and data handling. **Phase 1 (MVP - P2 priority)**: (1) `prlt chat` command with local Claude API for instant answers. (2) Search local docs/help files for answers. (3) Fallback to "Contact support" with mailto link or GitHub issue. (4) `prlt subscribe` to collect emails manually. **Phase 2 (Post-launch - 1k+ users)**: (1) Simple backend API for question queue. (2) Admin dashboard to see/answer questions. (3) Track common questions → improve docs. (4) Announcement system for push notifications. **Phase 3 (Scale - 10k+ users)**: (1) Fine-tuned Claude model on prlt-specific Q&A. (2) Real-time chat with typing indicators. (3) Community answers (users help each other). (4) Integration with usage analytics to proactively offer help (detect confusion patterns). **Unique differentiator**: No other CLI tool has this. GitHub Copilot CLI is close but doesn't offer human support fallback or bi-directional communication. This makes prlt feel more like a product with a team behind it, less like abandoned open source. **Cost structure**: Claude API calls for chat ~$0.01 per conversation. At 1k users with 10% using chat monthly = 100 conversations/mo = $1/mo. Scales cheaply. **Related to**: Feedback mechanism (line 14 - overlaps, might consolidate), better error messages (line 20 - chat can explain errors), interactive prompts (line 21 - similar UX pattern), usage analytics (line 17 - tracks what users ask about). **Benefits**: (1) **Users**: Instant help without context switching, better onboarding. (2) **You**: Learn pain points, build relationship with users, differentiation. (3) **Product**: Better docs, faster iteration, user loyalty. **Priority P2**: Not critical for MVP but powerful post-launch feature. Build after core functionality is solid and you have 500-1k users asking questions. Could be the thing that makes prlt sticky. |
| - | Star from CLI | backlog | P1 | feature | `prlt star` command and first-run prompt to star the repo on GitHub. Growth hack: prompt after successful `prlt init` with "⭐ Star prlt on GitHub? (helps others discover it)". Phase 1: opens browser to repo. Phase 2: use GitHub API to star directly if `gh` CLI available. Low-friction way to boost GitHub stars and credibility. |
| - | View and vote on roadmap from CLI | backlog | P1 | feature | Let users see what's coming and vote on features directly from CLI. **Commands**: (1) `prlt roadmap` or `prlt roadmap list` - Display upcoming features from ROADMAP.md, grouped by project, color-coded by priority (P0=red, P1=yellow, P2=blue). Show vote counts next to each feature. (2) `prlt roadmap vote <feature-id>` - Upvote a feature (e.g., `prlt roadmap vote github-issues-integration`). Stores vote remotely (requires opt-in analytics or GitHub auth). (3) `prlt roadmap view <feature-id>` - Show full details for a specific feature. (4) `prlt roadmap search <query>` - Search roadmap items by keyword. (5) `prlt roadmap --filter priority:P0` or `--filter category:feature` - Filter roadmap by priority/category/status. **Implementation**: (a) **Data source**: Fetch roadmap from GitHub (ROADMAP.md in main branch) or prlt API endpoint. Cache locally, refresh on command or weekly. (b) **Voting backend**: Simple API endpoint that stores votes with anonymized user ID (derived from machine + install hash). No PII. (c) **Display format**: Terminal table with columns: ID, Title, Priority, Votes, Status. Use colors for visual hierarchy. (d) **Opt-in**: Voting requires opt-in to analytics (respects privacy). Viewing roadmap works offline with cached data. **Benefits**: (1) Transparency - users see what's being built. (2) Community input - vote on features you want. (3) Discoverability - find upcoming features without leaving terminal. (4) Product insights - see what users actually want vs assume. (5) Engagement - keeps users connected to project evolution. **Related patterns**: npm roadmap, gh issue list, vercel roadmap. **Phase 1**: `prlt roadmap` shows static ROADMAP.md in terminal. **Phase 2**: Add voting with backend API. **Phase 3**: Show vote counts, sort by popularity. This builds community engagement and gives product signal without surveys. |
| - | Usage analytics (Statsig/PostHog) | backlog | P1 | feature | Integrate Statsig or PostHog for command usage analytics. What commands are used, success rates, feature adoption. Privacy-respecting (opt-in or anonymized). Critical for understanding what to build next post-launch. |
| - | Error tracking (Sentry) | backlog | P1 | feature | Integrate Sentry for error tracking. Capture unhandled exceptions, failed commands, stack traces. Context: command, args, environment. Privacy-respecting. Essential for debugging issues in production. |
| - | Auto-upgrade notification | backlog | P2 | feature | Check for new versions on startup. Suggest `prlt upgrade` if behind. Non-intrusive banner. Keeps users on latest version without forcing updates. |
| - | Better error messages | backlog | P2 | chore | More helpful error messages throughout CLI. Polish for launch. |
| - | Interactive "what's next?" prompts after commands | backlog | P2 | feature | After completing an action, prompt user with suggested next steps to keep momentum. **Examples**: (1) After `prlt ticket create`: "Ticket TKT-001 created. What next? [Assign to agent / Create another ticket / Spawn work / Exit]". (2) After `prlt agent add alice`: "Agent 'alice' added. What next? [Create ticket / Assign existing ticket / Add another agent / Exit]". (3) After `prlt work spawn TKT-001 alice`: "Work spawned for alice on TKT-001. What next? [Check status / Spawn more work / View logs / Exit]". **Benefits**: (a) Reduces cognitive load - users don't need to remember what comes next. (b) Guides new users through workflows. (c) Keeps flow state - no context switching to docs. (d) Teaches the tool through usage. **Implementation options**: (1) **Per-command**: Each command implements its own "what's next?" logic inline. (2) **Hook system**: Use hooks/callbacks (Project 4 ticket) - register post-action prompts in config. (3) **Workflow engine**: Define common workflows in YAML, auto-prompt based on workflow step. (4) **Opt-in flag**: Add `--interactive` or `-i` flag to enable prompts (off by default for scripting). **Design considerations**: Must not break scripting/CI usage - only show prompts in interactive TTY sessions. Respect `--yes` or `--quiet` flags. Allow `prlt config set interactive-prompts false` to disable globally. **Related patterns**: Git's "did you mean?" suggestions, npm's "create-*" wizards, Railway CLI's step-by-step flows. **Priority P2**: Nice DX improvement but not critical for launch - power users will know the commands, this helps beginners. Could be good post-launch polish based on user feedback about discoverability. |
| - | Standardize command structure (singular + flags) | backlog | P1 | refactor | Remove plural command directories (agents/, tickets/, works/, etc.) - keep only singular. Replace spawn-all with `spawn --all` flag. Consolidate batch operations as flags (--all, --column) not separate commands. Matches Git/Docker/K8s conventions. Breaking change for v1.0. |
| - | Define agent work completion criteria | backlog | P1 | feature | Define when agent work is considered "done" and what happens to agent/container/ticket status. **Questions to resolve during implementation**: (1) Ticket status updates: When agent creates PR, auto-update to "in-review"? When PR merged, auto-update to "done"? Manual completion only via `prlt ticket complete`? (2) Agent/container lifecycle: Auto-stop after PR created? Keep running for fixes? Configurable with flags? (3) Failure handling: What if agent gets stuck, tests fail, or can't solve ticket? Mark as "blocked"? Add comment? Timeout behavior? (4) Cleanup: Auto-remove worktrees/containers after completion or manual cleanup with `prlt agent clean`? (5) Resume workflow: Can work be resumed with `prlt work resume TKT-XXX` if agent stopped early? **Recommendation for MVP**: PR created → ticket to "in-review" status, agent auto-stops (save resources), worktree kept for reference, manual `prlt ticket complete TKT-XXX` after PR merged. Add automation (PR merge webhooks, auto-cleanup) in later iterations based on user feedback. Critical to define this workflow before launch - affects user experience and resource management. |
| - | PR and branch status sync via polling | backlog | P2 | feature | Auto-sync ticket and agent status based on GitHub state using polling (like `gh` CLI). Add `prlt sync` command with two responsibilities: (1) **PR status sync**: Poll GitHub API to check PR status for all tickets with linked PRs. When PR merged, auto-update ticket to "done". Store PR URL when agent creates PR (link ticket ↔ PR in database). (2) **Branch update detection**: Poll for new commits on agent branches to track agent activity/progress. Show last commit info and timestamp in `prlt agent list` or `prlt work list`. Detect if someone manually pushed to agent's branch. Requires GitHub token in config (`prlt config set github-token ghp_xxx`). **Phase 1**: Manual `prlt sync` command for both PR and branch updates. **Phase 2**: Auto-sync in background on `prlt ticket list` and `prlt agent list` (non-blocking, silent). **Phase 3** (optional): Watch daemon mode `prlt watch` that syncs every 5 minutes. Respects GitHub API rate limits (5000/hour with token, 60/hour without). Alternative: Could trigger via cron job (`*/5 * * * * prlt sync --quiet`). Future: Replace with webhooks if building SaaS/hosted version. This is CLI-appropriate solution - webhooks require server infrastructure which doesn't work with local SQLite database. Polling matches how other CLI tools (gh, terraform) handle remote state sync. Branch polling helps answer "is my agent still working?" and "what's the latest commit?" without needing to check containers or worktrees directly. |
| - | Backtest ticket quality (training data generation) | backlog | P2 | feature | Generate training data for improving ticket quality by comparing original tickets to final merged code. **Goal**: Create self-sufficient tickets that agents can complete autonomously without human revision/shepherding. **Process**: Add `prlt backtest` command that: (1) Analyzes completed tickets (status=done with linked PR). (2) Fetches original ticket description from database. (3) Fetches final merged code from PR via GitHub API. (4) Uses AI model (Claude, GPT-4) to compare: what was asked vs what was built. (5) Generates an "ideal ticket" that would have produced the final code on first try - identifies missing context, ambiguous requirements, unstated assumptions, architectural decisions that should have been specified. (6) Outputs comparison report: original ticket, final code diff, generated ideal ticket, gap analysis. (7) Stores results as training examples in `pmo/backtests/` directory or dedicated table. **Use cases**: (a) Improve your own ticket-writing over time by learning what details matter. (b) Build training dataset for fine-tuning models on prlt-specific ticket formats. (c) Create a "ticket quality score" metric based on how many revisions were needed. (d) Generate documentation: "Examples of good tickets" by showing before/after. **Phase 1**: Manual `prlt backtest TKT-XXX` for single ticket. **Phase 2**: Batch mode `prlt backtest --all --filter="status:done"` to analyze all completed work. **Phase 3**: Auto-backtest on PR merge (optional). Store in database with schema: `backtests(id, ticket_id, pr_url, original_ticket, final_diff, ideal_ticket, gap_analysis, created_at)`. Requires API key for AI model. Privacy: only processes your own code/tickets, stays local. This is meta-improvement: using AI to teach AI how to work better. |
| - | AI-assisted backlog refinement/grooming | backlog | P2 | feature | **[PLACEHOLDER - BRAINSTORMING]** AI-powered command to refine and groom backlog tickets. **Potential features**: (1) `prlt refine` or `prlt groom` command that analyzes backlog tickets. (2) Identifies tickets that are vague, missing context, or under-specified. (3) Suggests improvements: missing acceptance criteria, unclear requirements, architectural constraints needed. (4) Auto-generates questions to ask (what library? what pattern? edge cases?). (5) Estimates complexity/effort based on ticket description. (6) Detects duplicates or overlapping tickets. (7) Suggests breaking large tickets into subtasks. (8) Prioritization assistance based on dependencies, business value, effort. (9) Interactive mode: CLI walks through each backlog ticket, suggests edits, user accepts/rejects. (10) Batch mode: analyze all backlog, generate report of issues. **Use cases**: (a) Pre-work quality check before assigning to agents. (b) Backlog hygiene - keep tickets "agent-ready". (c) Learn what makes a good ticket through AI feedback. **Integration with backtest**: Use backtesting insights to train refinement suggestions (if tickets with X pattern failed, flag similar patterns). **Questions to resolve**: Should this modify tickets automatically or just suggest? Command vs interactive TUI? What AI model to use? **Related to**: Backtest ticket quality (teaches what good looks like), ticket templates (enforces structure). This is proactive ticket improvement vs reactive (backtest). |
| - | Agent conversation trajectory logging | backlog | P2 | feature | Capture full agent conversation history for training data and ticket quality analysis. **Goal**: Build dataset of (ticket → conversation → outcome) trajectories to power RL-based ticket optimization. **Problem**: Don't control Claude Code's conversation flow directly, need to instrument prlt to capture what we can. **Approach**: Multi-layered logging strategy: (1) **Docker output capture**: Log stdout/stderr from agent containers to `pmo/logs/TKT-XXX-agent.log`. Parse for patterns like "I need clarification on X" or tool calls. (2) **Agent environment instrumentation**: Add helper script in devcontainer that agents can call: `prlt-log "clarification_question" "Should I use localStorage?"`. Make it easy/optional for agents to self-report. (3) **Git history analysis**: Parse commit messages, PR descriptions, code diffs as proxy for conversation. If agent makes 5 commits with "fix X" messages, that's 5 iterations. (4) **Post-work parsing**: After ticket completion, analyze Docker logs + git history + PR comments to reconstruct conversation. Use AI to extract: questions asked, clarifications needed, decisions made, iterations required. (5) **Structured storage**: Store in database table `conversation_trajectories(ticket_id, turn_number, speaker, type, content, timestamp, metadata)`. Export to JSONL for training. **Metrics to track**: Total turns, clarification questions, code revisions, time to completion, human interventions. **Phase 1**: Capture Docker logs and git history (passive). **Phase 2**: Add agent reporting helpers (active but optional). **Phase 3**: Build parsing/analysis pipeline. **Phase 4**: Integrate with backtest to generate ideal tickets based on conversation patterns. **Use cases**: (a) Identify which ticket patterns trigger most questions. (b) Build predictive model: "This ticket will need 4 clarifications" (low quality score). (c) Train refinement suggestions: "Tickets without library spec consistently need clarification". (d) Generate training data for fine-tuning models. **Privacy**: All data stays local, opt-in export. This is **bidirectional RL**: training humans to write better tickets AND agents to ask better questions. |
| - | Fix better-sqlite3 native module build issues | backlog | P1 | bug | **Problem**: Tests fail with `dlopen(...better_sqlite3.node...): not a mach-o file` error. The native `.node` binary is incompatible with current Node/architecture. **Causes**: (1) Node version changed without rebuild. (2) Architecture mismatch (x86 vs ARM/M1). (3) node_modules copied from different machine/Docker. (4) pnpm link from incompatible location. **Current workaround**: `pnpm rebuild better-sqlite3` or `rm -rf node_modules && pnpm install`. **Proper fixes to implement**: (1) **postinstall script**: Add `"postinstall": "npm rebuild better-sqlite3"` to package.json to auto-rebuild on install. (2) **CI/CD verification**: Ensure GitHub Actions rebuilds native modules for each platform in matrix build. (3) **Documentation**: Add troubleshooting section for native module issues. (4) **Engine lockdown**: Pin Node version in `.nvmrc` and `package.json` engines field to prevent version drift. (5) **pnpm config**: Consider `shamefully-hoist=true` or other pnpm settings that affect native module resolution. (6) **Alternative**: Evaluate `sql.js` (pure JS SQLite) as fallback for environments where native modules are problematic - slower but zero native deps. Trade-off: performance vs portability. **Affected areas**: Test suite (apps/cli/test/), any command that uses database. **Related**: Cross-platform support (native modules behave differently on Linux/Windows/macOS). **Priority P1**: Blocks test suite, affects developer onboarding.  |
| - | Linux and Windows support | backlog | P0 | platform | Add full cross-platform support for Linux and Windows alongside current macOS support. **Current state**: prlt is developed and tested primarily on macOS. Many commands use macOS-specific paths, shell scripts, or assumptions that may not work on Linux/Windows. **Problem**: Limits adoption - most developers use macOS, but significant portion use Linux (especially backend/infra engineers) and Windows (especially with WSL2). Can't achieve broad adoption without cross-platform support. **Platform-specific issues to address**: (1) **Path handling**: Use `path.join()` consistently instead of hardcoded `/` separators. Handle Windows drive letters (`C:\`), different home directory conventions (`~/.proletariat` vs `%USERPROFILE%\.proletariat`). (2) **Shell/terminal differences**: macOS/Linux use bash/zsh, Windows uses PowerShell/cmd. Terminal escape sequences differ. Shell script invocations need abstraction. (3) **Docker integration**: Docker Desktop works differently on each platform. Windows requires WSL2 backend. Path mounting into containers differs (Windows paths need conversion for Docker). (4) **Git worktrees**: Different git path conventions. Windows may have issues with symlinks. (5) **File permissions**: Unix permissions vs Windows ACLs. Executable bits don't translate. (6) **Line endings**: CRLF (Windows) vs LF (Unix) - affects git operations and file writes. (7) **Environment variables**: Different conventions (`$HOME` vs `%USERPROFILE%`, `$PATH` vs `%PATH%`). (8) **Terminal capabilities**: Color support varies. Windows Terminal vs cmd vs PowerShell have different ANSI support. (9) **Process management**: Different process spawning, signal handling (SIGTERM on Unix doesn't exist on Windows). **Solution - Cross-platform abstraction layer**: (1) **Path utilities**: Create `src/lib/paths.ts` with functions like `getProletariatHome()`, `getGarageDir()`, `getConfigPath()`. Use Node's `path` and `os` modules. (2) **Shell abstraction**: Detect platform and choose appropriate shell. Use `execa` or similar for cross-platform process spawning. (3) **Terminal utilities**: Abstract terminal operations (colors, titles, escape sequences) with platform detection. Use libraries like `chalk` (cross-platform colors), `terminal-kit` (cross-platform terminal control). (4) **Docker path mapping**: Convert Windows paths to WSL/Docker-compatible paths when mounting volumes. (5) **Configuration**: Store platform-agnostic config. Auto-detect platform on `prlt init` and set defaults accordingly. **Testing strategy**: (1) **CI/CD**: Run tests on GitHub Actions with matrix: `os: [ubuntu-latest, windows-latest, macos-latest]`. (2) **Platform-specific test suites**: Mark tests that only apply to certain platforms or require platform-specific setup. (3) **Docker testing**: Test Docker integration on all platforms (Docker Desktop on Mac/Windows, native Docker on Linux). (4) **Manual testing**: Test on real machines (VM or physical) for each platform, not just CI. **Phase 1 (MVP)**: (1) Audit codebase for platform-specific code (search for hardcoded paths, shell commands). (2) Implement cross-platform path utilities. (3) Test on Linux (Ubuntu) - likely easiest since it's Unix-like. (4) Document known issues/limitations per platform. **Phase 2 (Post-MVP)**: (1) Full Windows support with WSL2. (2) Windows-native support (without WSL2) - much harder, may defer. (3) Package platform-specific installers (`.deb` for Debian/Ubuntu, `.msi` for Windows, `.dmg` for macOS). **Phase 3 (Polish)**: (1) Platform-specific documentation (installation guides per OS). (2) Auto-install scripts for each platform (`curl | sh` for Unix, PowerShell script for Windows). (3) Optimize performance per platform. **Priority P0**: Critical blocker for wide adoption. Currently limiting potential userbase to macOS developers only (~40% of devs). Linux support alone would unlock backend/infra/DevOps engineers (~35% of devs). Windows+WSL2 adds enterprise developers (~25% of devs). Can't call it "MVP" if it only works on one platform. Should tackle Linux first (easier, similar to macOS), then Windows. **Related to**: Docker execution safety (line 10 - Docker behaves differently per platform), documentation (line 8 - needs platform-specific install guides), E2E tests (line 152 - need cross-platform CI). **Success criteria**: User can run full prlt workflow on Ubuntu Linux and Windows 11 with WSL2 without errors. All e2e tests pass on all three platforms in CI. |

---

## Project 2: Credibility & Polish

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| - | Public issues board | backlog | P1 | docs | GitHub Issues enabled and organized. Public roadmap, bugs, feature requests. Shows transparency, active development. Let community contribute/vote on features. |
| - | Public roadmap board | backlog | P1 | docs | GitHub Projects board showing roadmap visually. This ROADMAP.md as a living board. Public visibility into what's next. Builds trust. |
| - | Logo and branding | backlog | P2 | chore | Professional appearance for first impressions. Logo, consistent colors, consistent visual identity. |
| - | Test coverage | backlog | P2 | test | Visible test suite builds trust. Unit tests, integration tests, e2e tests for core flows. |
| - | Changelog | backlog | P2 | docs | Track releases, show active development. CHANGELOG.md with semver releases. |
| - | Competitor landscape/leaderboard | backlog | P2 | docs | Public list of tools in the multi-agent orchestration space. Not competitive - educational. "Here's who's building in this space." Signals awareness, confidence. Could be page on site or curated awesome-list. |
| - | Content calendar for LI/Threads/Blog | backlog | P2 | docs | Plan content cadence with detailed feature explanations for each prlt capability. **Content types**: (1) **Feature explainers** - One post per feature explaining what it does, why it matters, how to use it. (2) **Launch announcement** - What is prlt, the vision, call to action. (3) **Use cases** - Real scenarios where prlt shines. (4) **Technical deep dives** - Architecture, design decisions, lessons learned. (5) **Progress updates** - What's new, what's coming. **Feature explainer content (one post each)**: (1) `prlt init` - Setting up your HQ, workspace structure, why opinionated organization matters. (2) `prlt agent add` - Adding AI agents to your team, themes, agent naming. (3) `prlt ticket create` - Ticket-driven development, writing good tickets for agents. (4) `prlt work spawn` - Spawning work in containers, isolation, Docker integration. (5) `prlt board` - Kanban workflow, columns, moving tickets through stages. (6) `prlt spec` - Specs as context, linking specs to tickets, structured requirements. (7) PMO structure - board.md, specs/, how prlt organizes project management. (8) Git worktrees - How agents work in isolated branches, worktree management. (9) Devcontainers - Reproducible environments, Claude Code in containers. (10) Multi-repo support - Managing multiple repos in one HQ. (11) Agent themes - Billionaires theme, customization, personality. (12) PR workflow - Agent creates PR, human reviews, merge flow. **Platform strategy**: LinkedIn (professional/technical), Threads (casual/updates), Twitter/X (dev community), Blog (long-form deep dives), YouTube (demos/tutorials). **Cadence**: 2-3 posts/week across platforms. One feature explainer per week. **Format ideas**: (1) Short video demos (< 60s) showing command in action. (2) Before/after comparisons (manual vs prlt workflow). (3) Code snippets with explanations. (4) Architecture diagrams. (5) "Did you know?" tips. (6) User questions answered. **Tracking**: GitHub Projects board with columns: Ideas → Writing → Review → Scheduled → Published. Or Notion/spreadsheet. **Priority P2**: Important for adoption but not blocking launch. Start creating content in parallel with MVP work. Have launch content ready before going public. |
| - | Testimonials and case studies | backlog | P3 | docs | Social proof. "I used prlt to build X" stories. Even self-testimonial of dogfooding prlt to build prlt. Collect user stories as adoption grows. |

---

## Project 3: Open Ecosystem (Key Differentiator)

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| - | GitHub Issues integration | backlog | P1 | feature | Sync prlt with GitHub Issues. Commands: `prlt github connect`, `sync`, `import`, `export`. Map: tickets ↔ issues, statuses ↔ labels/state. Bidirectional sync - work on GH issues via prlt, auto-update with PR links and status changes. Makes prlt a layer on top of existing workflow, not replacement. Largest user base (every dev has GitHub), easiest API, proves integration pattern for Linear/Jira/GitLab. Key differentiator: built-in PMO OR use existing tools. **Design decisions to make during implementation**: (1) Status mapping: GitHub open/closed vs prlt's backlog/planned/in-progress/done/canceled - use labels for granularity or simplify to open=ready, closed=done? (2) Repo mapping: one prlt project per repo, or multi-repo projects? (3) Sync direction: prlt→GH only (simple), GH→prlt only (import), or bidirectional (complex)? (4) Conflict resolution: if status changes in both places, which wins? Start simple (prlt→GH, labels for status), iterate based on feedback. |
| - | Linear integration | backlog | P1 | feature | Sync tickets/projects with Linear. Commands: `prlt linear connect`, `sync`, `import`, `export`. Map: tickets ↔ issues, statuses ↔ workflow states, projects ↔ projects. Bidirectional sync. Modern teams use Linear - high-value integration. Proves enterprise-ready integration capabilities. |
| - | GitLab Issues integration | backlog | P1 | feature | Sync prlt with GitLab Issues. Commands: `prlt gitlab connect`, `sync`, `import`, `export`. Similar to GitHub integration but with richer feature set: issue weights, time tracking, dependencies, epics. Important for enterprise/self-hosted users. GitLab's PM features are more advanced than GitHub's (built-in boards, dependencies, estimates). Second-largest git platform (~30M users). |
| - | Multi-provider agent support (Codex, Gemini CLI) | backlog | P1 | feature | Support spawning agents with different AI backends beyond Claude Code. Codex CLI (OpenAI), Gemini CLI (Google), others. Agent config specifies provider. Devcontainer setup per provider. Key for adoption - not locked to one vendor. Provider-agnostic prompt/context passing. |
| - | MCP server for prlt | backlog | P1 | feature | Expose prlt as an MCP tool. Other AI agents can create tickets, spawn work, check status. Meta: agents orchestrating agents. |
| - | Import/export (CSV, JSON) | backlog | P2 | feature | Get data in/out easily. Export tickets/projects to CSV/JSON. Import from other tools. Reduces lock-in. |
| - | Plugin/extension system | backlog | P2 | feature | Let others add providers, integrations, custom commands. Plugin API, discovery, loading. Key for ecosystem growth. |
| - | Webhook support | backlog | P2 | feature | Trigger prlt from external events. Receive webhooks for ticket updates, PR merges, etc. Enables automation. |
| - | API for programmatic access | backlog | P2 | feature | REST or gRPC API beyond CLI. Enables integrations, dashboards, custom tooling. |
| - | Open source strategy and auto-publish to public repo | backlog | P1 | infra | Investigate and implement open source publishing workflow for prlt CLI. **Goals**: (1) Make prlt publicly available as open source. (2) Automate syncing from private dev repo to public repo. (3) Choose right license and structure for community + commercial viability. **License options to evaluate**: (1) **MIT** - Maximum adoption, no restrictions, anyone can fork/commercialize. Used by: React, Vue, Express. Pro: Widest adoption. Con: Competitors can take everything. (2) **Apache 2.0** - Like MIT but with patent protection. Used by: Kubernetes, Terraform, Rust. Pro: Enterprise-friendly, patent clause. Con: Slightly more complex. (3) **AGPL** - Copyleft, requires sharing modifications if used as service. Used by: MongoDB (originally), Grafana. Pro: Prevents cloud providers from taking without contributing. Con: Scares some enterprises. (4) **BSL (Business Source License)** - Source-available, converts to open source after time delay. Used by: MariaDB, Sentry, CockroachDB. Pro: Protects commercial interests while being transparent. Con: Not "true" open source, some community pushback. (5) **FSL (Functional Source License)** - Similar to BSL, Sentry's newer approach. **Recommendation**: Apache 2.0 or MIT for maximum adoption. If worried about AWS/cloud providers offering "prlt-as-a-service", consider BSL with 3-year conversion. **Repo structure options**: (1) **Single public repo** - Everything in one place. Simple. What most projects do. (2) **Monorepo with public/private split** - Public: CLI, SDK, docs. Private: cloud/enterprise features. (3) **Separate repos** - `prlt/cli` (public), `prlt/cloud` (private), `prlt/enterprise` (private). **Auto-publish workflow**: GitHub Actions workflow that: (1) Triggers on release tag in private repo. (2) Strips any private/internal code or secrets. (3) Pushes to public repo. (4) Creates GitHub Release with changelog. (5) Publishes to npm. (6) Updates Homebrew formula. **What to include in open source**: CLI core, all commands, SDK, documentation, themes, basic integrations. **What to keep private (if anything)**: Cloud infrastructure, enterprise auth/SSO, billing, internal tooling. **Community setup**: (1) CONTRIBUTING.md with guidelines. (2) CODE_OF_CONDUCT.md. (3) Issue templates (bug, feature request). (4) PR template. (5) Discussion forum (GitHub Discussions). (6) Discord or Slack for community chat. **DCO vs CLA**: Consider Developer Certificate of Origin (DCO) for contributions - lighter than CLA, signals contributors own their code. **Priority P1**: Critical for adoption strategy. Open source is the go-to-market, not an afterthought. Should be ready at launch. |
| - | Extract trajectory SDK for cross-platform RLHF | future | P2 | feature | Extract conversation trajectory logging into standalone SDK (`@proletariat/trajectory-sdk`) that other agent orchestration tools can integrate. **Vision**: Create cross-platform data network for RLHF training data on coding tasks. **Target integrations**: prlt, Sweep, Cursor, Replit Agent, Devin, other agent orchestration tools. **Value proposition**: (1) **For platforms**: Drop-in SDK to capture (ticket → conversation → outcome) trajectories without building infrastructure. (2) **For users**: Better agents across all tools through shared learning - insights from Cursor improve Sweep, and vice versa. (3) **For ecosystem**: Rising tide lifts all boats - everyone benefits from larger training dataset. (4) **For prlt**: First-mover advantage, ecosystem leadership, potential platform business. **SDK capabilities**: Standardized trajectory format (JSONL schema), hooks for capturing: ticket/spec input, conversation turns, tool calls, code changes, outcomes (PR merged, tests passed), metadata (time, model, context). Privacy-preserving: local capture with opt-in sharing. Export formats for different training frameworks. **Phased approach**: (1) **Phase 1 (0-6 months)**: Build into prlt, prove value, iterate on format and insights. Dogfood internally. (2) **Phase 2 (6-12 months)**: Extract as standalone SDK once format stabilizes. Open source with permissive license (MIT/Apache). Document integration guide. (3) **Phase 3 (12+ months)**: If adoption happens, build platform layer - aggregation service, analysis tools, marketplace for training data. **Business models**: (a) Open core - SDK free, hosted aggregation/analysis paid. (b) Data marketplace - users opt-in to share anonymized trajectories for compensation. (c) SaaS - host the infrastructure, platforms pay per integration. **Strategic value**: This creates a moat through data network effects - more platforms → more trajectories → better insights → more valuable to integrate. prlt becomes infrastructure layer for the agent orchestration ecosystem. **Dependencies**: Requires conversation trajectory logging (Project 1) to be built and validated first. **Risk**: Coordination challenge - need buy-in from other platforms. Competitive dynamics if platforms view data as proprietary. Privacy concerns around data sharing. Start by proving value internally before evangelizing externally. |

---

## Project 4: Extensibility

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| - | Hooks/callbacks on events | backlog | P2 | feature | Run custom scripts on events: ticket created, PR merged, agent started, etc. `prlt.hooks.js` or config-based. |
| - | Template system | backlog | P2 | feature | Ticket templates, spec templates, agent templates. Reduce boilerplate, enforce standards. |
| - | Config file (.prltrc) | backlog | P2 | feature | Project-level config file for customization. `.prltrc` or `prlt.config.js`. Defaults, hooks, preferences. |
| - | Custom commands | backlog | P3 | feature | User-defined commands via `prlt x mycommand`. Scripting interface for custom workflows. |
| - | Workflow automation (YAML scripts) | backlog | P3 | feature | Define multi-step workflows in YAML. `prlt run-workflow deploy.yaml`. Chain commands, conditionals, parallel steps. Like GitHub Actions but for prlt operations. |

---

## Project 5: Core PMO Refinement

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| TKT-040 | Implement Linear-style ticket states | in-progress | P0 | feature | Fixed categories (Backlog, Planned, In Progress, Done, Canceled) with custom statuses within each. Replaces current column-only model. |
| TKT-043 | Implement cross-entity dependencies | in-progress | P0 | feature | Generalized dependency table across entity types. Specs, tickets, epics can all depend on each other. Keep as separate domain. |
| - | Configurable workflow statuses | backlog | P1 | feature | Allow users to configure and customize workflow statuses per project instead of using hardcoded statuses. **Current state**: Fixed statuses (Backlog, Planned, In Progress, Done, Canceled) from TKT-040 implementation. Works for most workflows but doesn't fit all teams/processes. **Problem**: Different teams have different workflows - some need "In Review", "QA", "Deployed" statuses; some need simpler "Todo/Done"; some need complex multi-stage pipelines. Hardcoded statuses force teams to adapt to prlt instead of prlt adapting to teams. **Solution**: Add commands to customize workflow per project: (1) `prlt workflow init <template>` - Start with template (simple, standard, kanban, scrum, devops) or custom. (2) `prlt workflow add-status <name> --category <category>` - Add custom status (e.g., "In Review" in "In Progress" category). (3) `prlt workflow remove-status <name>` - Remove unused status. (4) `prlt workflow reorder <status> --position <n>` - Change status order in kanban view. (5) `prlt workflow list` - Show current workflow configuration. (6) `prlt workflow reset` - Reset to default Linear-style statuses. **Storage**: Store in `projects` table as `workflow_config JSON` column with structure: `{statuses: [{name: "Backlog", category: "backlog", position: 1}, ...]}`. Category still required (backlog/planned/in-progress/done/canceled) for system logic (what's active work? what's completed?), but display name and granularity is customizable. **Templates**: (a) **Simple** - Todo, Done. (b) **Standard** - Backlog, Todo, In Progress, Done. (c) **Kanban** - Backlog, Ready, In Progress, Review, Done. (d) **Scrum** - Backlog, Sprint Backlog, In Progress, Review, Done, Canceled. (e) **DevOps** - Backlog, Development, Code Review, QA, Staging, Production, Canceled. **Benefits**: (1) Fits different team workflows. (2) Matches existing processes (easier adoption). (3) More granular status tracking (In Review vs generic In Progress). (4) Better integration with external tools (map Linear/Jira/GitHub statuses accurately). **Phase 1**: Templates with `prlt workflow init`. **Phase 2**: Full CRUD on statuses. **Phase 3**: Per-project workflow isolation (different projects, different workflows). **Related to**: TKT-040 (implements base statuses), GitHub/Linear integration (needs status mapping), board views (displays custom statuses). Priority P1 since flexibility is key for adoption - teams won't adopt if workflow doesn't match their process. |
| - | Board views (one board + multiple views) | backlog | P1 | feature | Decouple views from board concept. One ticket pool per project with project-level statuses (not team-level like Linear - projects own their workflow, people rotate across projects). Multiple view types: kanban (status columns), list (sortable table), calendar (by due_date), eisenhower (priority × urgency grid). For eisenhower view: add `due_date TIMESTAMP` and `urgent BOOLEAN` fields. Urgency derived from urgent=true flag OR due_date proximity (<24h = urgent). Priority = importance. Current: board = kanban view only. |
| - | Add notes/comments to specs and tickets | backlog | P1 | feature | No place to capture discussion/decisions attached to work items. Need activity log or comment system. |
| - | Add labels system for flexible categorization | backlog | P2 | feature | Separate from priority. Labels for: categorization (bug, feature, ux, security), custom workflows (blocked, needs-review, quick-win). User-defined, flexible. Priority stays fixed levels, labels provide flexibility. Matches Linear pattern. |
| - | Add effort/estimate field to tickets | backlog | P2 | feature | Story points, t-shirt sizes (S/M/L/XL), or time estimates. Helps with planning and velocity tracking. |
| - | Ticket sorting and ordering | backlog | P2 | feature | Define consistent default sort order across views. Current inconsistency: Board uses column+position, ticket list uses title alphabetically, specs use position+created_at, epic selection uses status+title. Need to decide on sensible defaults - probably status first (active work visible), then priority, then ticket number (recency). |
| - | Project-level settings | backlog | P2 | feature | Implement or defer? Settings per project vs global. |
| - | Roadmap/timeline as first-class entity | backlog | P2 | feature | Roadmap = collection of projects/phases with timeline. Like this ROADMAP.md file but in the PMO. `prlt roadmap create`, `roadmap list`, `roadmap show`. Projects belong to roadmap phases. Timeline view shows projects/epics on horizontal axis with dependencies. Gantt is more granular (task-level); roadmap is strategic (project/epic-level). Linear has Roadmap view, Jira has Roadmap + Advanced Roadmaps for Gantt-style. |
| TKT-041 | Consolidate work/spawn commands | in-progress | P2 | refactor | Current: `work start` (single), `work spawn` (batch), `spawn-all` (redundant). Proposal: kill spawn-all.ts, maybe add `--all` flag to start. Low priority - current naming is fine. |

---

## Project 6: Agent System

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| TKT-042 | Make agent themes optional + customizable | needs-revision | P1 | feature | NEEDS REVISION. Don't remove themes - make them optional. Allow custom themes. Ship with built-in themes (billionaires, etc.) but don't force. |
| TKT-047 | Docker management CLI commands | backlog | P1 | feature | `prlt docker list` (show containers from agent_work), `prlt docker clean` (remove orphaned), `prlt docker status` (check daemon). |
| TKT-048 | Test PMO access in devcontainer | done | P0 | test | Verify PMO is properly mounted. Run ticket list/create from inside container. |
| - | Enforce one-ticket-per-container architecture | backlog | P0 | architecture | **Decision**: Enforce 1:1 mapping of tickets to Docker containers in work spawning system. One ticket per container, not multiple tickets packed into single container. **Rationale**: (1) **Simplicity**: Clean isolation, simple lifecycle (spawn → work → done → cleanup). Easy to reason about system state. (2) **Cost efficiency**: Packing multiple tickets per container doesn't actually save resources - you still need headroom for peak usage across all tickets. 1:1 mapping is actually cheaper in practice ($9.60/month vs $48/month in realistic scenario with bursty workloads). (3) **Resource management**: Clear resource limits per ticket - each ticket gets dedicated CPU/memory allocation. No interference between tickets. (4) **Debugging**: Easy to trace logs, inspect state, debug issues when one container = one ticket. (5) **Failure isolation**: If one ticket's work crashes or hangs, doesn't affect other tickets. Graceful degradation. (6) **Industry standard**: Matches proven patterns from GitHub Actions (one VM per test run), AWS Lambda (one invocation per container), Kubernetes (one primary process per container). (7) **Billing clarity**: Usage tracking and cost attribution is straightforward when ticket maps 1:1 to container. **Architecture**: Local mode spawns one Docker container per ticket via `prlt work spawn TKT-001 alice`. Cloud mode uses bin packing strategy at VM level (multiple containers per VM for efficiency) but maintains 1:1 ticket-to-container mapping. Never pack multiple tickets into single container. **Implementation**: (1) Validate in `work spawn` command that ticket isn't already assigned to running container. (2) Container naming convention: `prlt-agent-{agentName}-{ticketId}` (enforces 1:1 at infrastructure level). (3) Database schema: `agent_work` table links one ticket_id to one container_id (unique constraint). (4) Cloud mode: VM orchestrator (K8s, ECS, etc.) bin-packs containers onto VMs for efficiency, but each container still handles exactly one ticket. **Documentation**: Update SYSTEM.md and architecture docs to explain this decision and rationale. Include cost analysis showing why 1:1 is more efficient than packing. Reference industry examples. **Related tickets**: TKT-046 (Docker execution safety), TKT-047 (Docker management commands), Cloud Mode architecture design (Project 9). **Priority P0**: Foundational architecture decision that affects all work spawning logic. Must be enforced before launch to prevent technical debt and user confusion. Critical for both local and cloud modes. |
| - | Claude Code wrapper for agent automation | backlog | P1 | feature | Create wrapper commands around Claude Code CLI to enable automated agent workflows and programmatic control. **Problem**: Need to orchestrate Claude Code agents programmatically - send prompts, wait for completion, retrieve results, handle errors. Raw `claude` CLI is interactive and not designed for automation. **Goal**: Enable `prlt` to spawn agent work by sending ticket context to Claude Code and managing the conversation programmatically. **Commands to add**: (1) `prlt claude send <agent> <message>` - Send message/prompt to Claude Code instance running in agent's container. Non-blocking, queues message. (2) `prlt claude exec <agent> <message>` - Send message and wait for completion (blocking). Returns output when Claude finishes. (3) `prlt claude status <agent>` - Check if Claude is idle, working, or waiting for input. (4) `prlt claude attach <agent>` - Attach to agent's Claude session interactively (like `docker attach`). (5) `prlt claude logs <agent>` - View conversation history/transcript for agent. (6) `prlt claude kill <agent>` - Stop Claude session in agent's container. **Implementation approaches**: (1) **File-based communication** (simplest): Write prompts to mounted file (e.g., `/workspace/.prlt/inbox.txt`), Claude Code reads on startup or via watcher. Write responses to `/workspace/.prlt/outbox.txt`. Polling-based, no real-time interaction. (2) **Named pipes/FIFO** (Unix): Create named pipe in container, write prompts to pipe, Claude reads from pipe. Bidirectional communication. Works well on Linux/macOS, not Windows. (3) **Docker exec with stdin** (moderate): Use `docker exec -i <container> claude` and pipe stdin/stdout. Send prompts via stdin, read responses from stdout. Parse Claude's output to detect completion. (4) **Claude Code API mode** (ideal, future): If Claude Code adds headless/API mode, use that. Send JSON-RPC or REST requests. Most robust but requires upstream changes to Claude Code. (5) **Wrapper script in container** (recommended for MVP): Include helper script `/usr/local/bin/prlt-claude` in devcontainer that wraps `claude` command. Script reads from `/workspace/.prlt/inbox.jsonl` (JSONL queue of prompts), executes each with Claude, writes results to `/workspace/.prlt/outbox.jsonl`. Daemon mode: `prlt-claude --daemon` runs in background, processes queue continuously. **Message format** (JSONL for simplicity): ```json {"id": "msg-001", "timestamp": "2025-01-06T10:30:00Z", "prompt": "Read TKT-040 spec and implement the feature", "context": {"ticket_id": "TKT-040", "spec_path": "pmo/specs/spec-001.md"}} ``` Response: ```json {"id": "msg-001", "status": "completed", "timestamp": "2025-01-06T11:45:00Z", "output": "Feature implemented. Created PR #123.", "files_changed": ["src/feature.ts"], "pr_url": "https://github.com/.../pull/123"} ``` **Work spawn integration**: When running `prlt work spawn TKT-001 alice`, the flow becomes: (1) Spawn Docker container with devcontainer for alice. (2) Mount PMO directory for ticket/spec access. (3) Start `prlt-claude --daemon` in container (background process). (4) Queue initial prompt: "Read ticket TKT-001 from pmo/tickets.db and complete the work. Create PR when done." (5) Poll for completion by watching outbox.jsonl. (6) When Claude responds with PR URL, update ticket status to "in-review". **Benefits**: (1) **Automation**: Spawn multiple agents in parallel, send work, track progress programmatically. (2) **Monitoring**: Check agent status without manual inspection. (3) **Debugging**: Review conversation logs to understand what agent did. (4) **Interruption**: Send follow-up instructions ("pause and run tests first"). (5) **Scalability**: Foundation for cloud mode where hundreds of agents work simultaneously. **Error handling**: (1) Claude errors (syntax, API failures) logged to outbox with status="error". (2) Timeouts: If no response after N minutes, mark as "stalled", allow manual intervention. (3) Human-in-the-loop: `prlt claude attach` lets human take over if agent gets stuck. **Phase 1 (MVP)**: File-based queue system with wrapper script in devcontainer. `prlt claude send/exec/status/logs` commands. **Phase 2**: Real-time communication via pipes or websockets. **Phase 3**: Integration with Claude Code team for official API/headless mode. **Priority P1**: Critical for making agent work spawning actually useful. Without this, agents are just containers with Claude installed but no way to orchestrate them. Blocks full automation of ticket → agent → PR workflow. **Related tickets**: Agent notification system (similar communication pattern), Docker execution safety (TKT-046), work spawn commands (TKT-041), agent conversation trajectory logging (Project 1 - uses this for capturing logs). **Alternative consideration**: If Claude Code adds native daemon/API mode, this ticket becomes simpler - just wrap their API. Until then, we need to build orchestration layer ourselves. |
| - | Set terminal tab name when opening agent workspace | backlog | P2 | feature | When opening a new terminal for an agent workspace (via `prlt work open` or similar command), automatically set the terminal tab/window title to the agent name for easy identification. **Problem**: When working with multiple agents across different terminal tabs, it's hard to tell which tab belongs to which agent workspace. Need to manually check pwd or look at prompt. **Solution**: Use ANSI escape sequences to set terminal title when opening agent workspace terminal. Different implementations per terminal: (1) **macOS Terminal/iTerm2**: `echo -n -e "\033]0;prlt: agent-name\007"`. (2) **Linux terminals** (gnome-terminal, xterm, konsole): Similar escape sequence `\033]2;prlt: agent-name\007`. (3) **Windows Terminal**: PowerShell `$Host.UI.RawUI.WindowTitle = "prlt: agent-name"`. (4) **tmux**: `tmux rename-window "agent-name"` if in tmux session. **Commands to add**: (1) `prlt work open <agent-name>` - Opens new terminal tab/window in agent's workspace directory with title set. Uses `open -a Terminal` on macOS, `gnome-terminal` on Linux, etc. (2) `prlt work shell <agent-name>` - Starts new shell in current terminal but sets title. (3) Auto-set title in existing `prlt work start` flow if spawning interactive shell. **Implementation**: (a) Detect terminal type from `$TERM` and `$TERM_PROGRAM` env vars. (b) Apply appropriate escape sequence or command. (c) Fallback gracefully if terminal doesn't support title setting. (d) Add config option to disable: `prlt config set terminal.set-title false`. **Related patterns**: `kubectl exec`, `docker exec`, `ssh` all set terminal titles. VS Code sets titles for integrated terminals. **Benefits**: Easily identify which terminal belongs to which agent when juggling 5-10 agent workspaces. Visual organization. Matches IDE behavior (VS Code shows workspace name in terminal title). **Priority P2**: Nice quality-of-life improvement but not critical for MVP. Helps with multi-agent workflows once you're actively using multiple agents. |
| - | Open IDE in agent workspace with preferred editor | backlog | P2 | feature | Command to open preferred IDE/editor in agent workspace directory with AI coding assistant activated. **Goal**: One command to jump into agent's workspace with full IDE setup, ready to review agent's work or manually intervene. **Command**: `prlt work edit <agent-name>` or `prlt work ide <agent-name>`. Opens the configured editor in agent's workspace directory. **Editor support**: (1) **VS Code** (most common): `code <workspace-path>` - opens VS Code. If user has GitHub Copilot or Claude Code extension installed, those should be available immediately. Could pass flags: `code <path> --new-window` (always new window), `code <path> --reuse-window` (reuse existing). (2) **Cursor**: `cursor <workspace-path>` - Cursor IDE with built-in AI. (3) **Zed**: `zed <workspace-path>` - Zed with AI features. (4) **JetBrains IDEs** (IntelliJ, WebStorm, PyCharm): `idea <workspace-path>`, `webstorm <workspace-path>`, `pycharm <workspace-path>`. (5) **Neovim/Vim**: `nvim <workspace-path>` or `vim <workspace-path>`. (6) **Emacs**: `emacs <workspace-path>`. **Configuration**: Store preferred editor in config: `prlt config set editor.command "code"` or `prlt config set editor.command "cursor --new-window"`. Auto-detect common editors if not configured: check for `code`, `cursor`, `zed`, `idea` in PATH. **Advanced features (Phase 2)**: (1) **Auto-activate AI assistant**: For VS Code, could use CLI flags or workspace settings to ensure Claude Code/Copilot is active. (2) **Load agent context**: Open the ticket description, spec file, or recent commits in editor. (3) **Split view**: Open both agent workspace AND main HQ repo side-by-side for comparison. **Implementation**: (a) Detect installed editors by checking PATH for common binaries. (b) Respect user config for preferred editor. (c) Fall back to `$EDITOR` environment variable if set. (d) Gracefully handle if no GUI editor available (headless server) - show helpful message. **Related patterns**: `gh repo view --web` (opens browser), `git open` (opens repo in browser), `railway open` (opens project dashboard). CLI tools launching GUI apps for richer interaction. **Benefits**: (1) Seamless workflow: `prlt work spawn alice TKT-001 && prlt work ide alice` to spawn work and immediately review in full IDE. (2) No manual `cd garage/alice && code .` steps. (3) Consistent with "prlt as orchestration layer" - it handles launching the right tools. (4) Supports human-in-the-loop workflows: agent does bulk work, human reviews/refines in IDE. **Priority P2**: Quality-of-life feature that smooths the workflow but not essential for core functionality. Becomes more valuable as users actively manage multiple agents and need to switch contexts frequently. |
| - | Agent theme marketplace | backlog | P2 | feature | Community marketplace for sharing and discovering custom agent themes. **Vision**: Let users create, share, and install agent themes beyond built-in options (billionaires, etc.). Build community engagement and creative expression around agent personalization. **Commands**: (1) `prlt theme browse` or `prlt theme marketplace` - Browse available community themes from registry (like npm). Show name, author, description, download count, rating. Filter by category (tech, historical, fictional, etc.). (2) `prlt theme install <theme-name>` - Install theme from marketplace (e.g., `prlt theme install anime-characters`). Downloads theme definition to `~/.proletariat/themes/`. (3) `prlt theme publish` - Publish custom theme to marketplace (requires auth). (4) `prlt theme list --local` - Show locally installed themes. (5) `prlt theme list --installed` - Show themes currently in use by agents. (6) `prlt theme create <name>` - Scaffold new theme template for development. (7) `prlt theme validate <theme-file>` - Validate theme definition before publishing. **Theme structure**: JSON/YAML file defining agent name pool, personality traits, commit message style, optional avatar URLs. Example: `{ "name": "anime-characters", "version": "1.0.0", "author": "username", "description": "Popular anime character names", "agents": ["goku", "naruto", "luffy", "ichigo", ...], "commitPrefix": "[anime]", "avatarUrls": {...} }`. **Marketplace backend options**: (1) **npm-based**: Publish themes as npm packages (`@prlt-themes/anime-characters`). Simple, uses existing infrastructure. Install with `npm install -g @prlt-themes/X`. (2) **GitHub registry**: Store themes as GitHub repos, install via URL. Decentralized, no server needed. (3) **prlt registry**: Custom registry API hosted on prlt servers. Most control, requires infrastructure. (4) **Hybrid**: Start with GitHub repos, migrate to custom registry if adoption grows. **Discovery & quality**: (1) **Curation**: Featured themes, trending themes, editor picks. (2) **Ratings**: User ratings and reviews. (3) **Stats**: Download counts, last updated, compatibility version. (4) **Search**: Tags, categories, keyword search. (5) **Quality gate**: Validation rules (no offensive content, min 10 names, valid format). **Monetization potential (future)**: (1) **Premium themes**: Paid themes with revenue split (80/20 creator/prlt). (2) **Sponsorships**: Featured placement for sponsored themes. (3) **Pro features**: Advanced theming capabilities (dynamic names, AI-generated personalities) behind paywall. **Community features**: (1) **Theme packs**: Collections of related themes (e.g., "Tech Giants Bundle"). (2) **Randomizer**: `prlt theme random` picks random theme from marketplace. (3) **Theme of the month**: Highlight community creations. (4) **Leaderboard**: Most popular theme creators. **Related to**: TKT-042 (make themes optional/customizable - prerequisite), plugin system (Project 3 - themes are like plugins). **Benefits**: (1) **Community engagement**: Users contribute themes, build ecosystem. (2) **Differentiation**: Unique feature other dev tools don't have. (3) **Virality**: Cool themes shared on social media drive discovery. (4) **Data insights**: See what themes are popular (product/market signal). (5) **Retention**: Users invested in customization are stickier. **Examples of successful marketplaces**: VS Code extensions, Raycast store, Obsidian themes, Zed themes. **Priority P2**: Fun differentiator but not critical for MVP. Build after core agent theming works (TKT-042) and there's proven demand for customization. Could be great post-launch feature to drive community growth. **Phase 1**: Manual theme sharing via GitHub gists/repos. **Phase 2**: CLI commands for install/browse using GitHub. **Phase 3**: Dedicated registry with web UI. |
| - | Agent status tracking | deferred | P3 | feature | Track working/idle/offline status. Deferred for MVP - can add later if needed. |
| - | CD into agent directory command | backlog | P2 | feature | Command to quickly navigate into an agent's workspace directory. **Command**: `prlt agent cd <agent-name>` or `prlt work cd <agent-name>`. Changes current working directory to agent's worktree path. **Problem**: Currently need to manually navigate to agent directories (e.g., `cd agents/staff/altman/my-repo`). Tedious when switching between agents frequently. **Implementation options**: (1) **Shell function** (recommended): Since `cd` affects parent shell, can't be done in subprocess. Generate shell function that user sources: `eval "$(prlt agent cd --shell)"` adds `prlt-cd` function to shell. Or `prlt agent cd alice` outputs the path and user wraps with `cd $(prlt agent cd alice)`. (2) **Subshell**: `prlt agent cd alice` spawns new shell already in that directory. Less ideal - creates nested shells. (3) **Print path only**: `prlt agent path alice` just prints the path. User does `cd $(prlt agent path alice)`. Most portable. **Shell integration**: Add to `.zshrc`/`.bashrc`: `prlt-cd() { cd "$(prlt agent path "$1")" }`. Then user can run `prlt-cd alice`. Or create alias: `alias pcd='cd $(prlt agent path'` → `pcd alice)`. **Tab completion**: Add shell completions for agent names so `prlt agent cd <TAB>` shows available agents. **Related**: Set terminal tab name (line 106), open IDE in workspace (line 107) - all about quick navigation to agent workspaces. **Priority P2**: Quality-of-life feature for multi-agent workflows. |

---

## Project 7: Developer Experience

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| TKT-053 | Commit namespace/prefix configuration | backlog | P2 | feature | Add configurable prefix for agent commits. e.g., `[prlt]` or `[prlt:altman]`. Helps identify agent-generated commits in git history. |
| TKT-054 | Branch naming with human coder name | backlog | P2 | feature | Include human coder name in branch. Current: `feat/altman/TKT-040-...`. Proposed: `feat/chris/TKT-040-...` or `feat/chris/altman/TKT-040-...`. |
| - | Auto-seed initial commit with ticket info | backlog | P2 | feature | When creating branch with ticket ID in name (e.g., `fix/agent/TKT-052-...`), automatically create initial commit with message "TKT-052: [ticket title]". Add `--seed-commit <message>` flag to override with custom message. If `--seed-commit` provided without value, use "Initial commit". Default behavior: auto-detect ticket ID from branch name and seed with ticket info. This seeds PR title and provides context. Update `prlt branch create` command and ticket template to reflect this behavior. |
| - | Git branch aliases for agent worktrees | backlog | P2 | feature | Set up git aliases in agent worktrees for common operations. Examples: `git co` → `git checkout`, `git st` → `git status`, `git br` → `git branch`. Configure via `.gitconfig` in each worktree or global config. Improves agent workflow ergonomics. Optional: let users define custom aliases in prlt config that get applied to all agent worktrees. |
| - | Better input UX for description fields | backlog | P2 | feature | Replace vim-based description editing with better UX. Multi-line text input, inline editing, or prompts library. Vim opens in terminal and is jarring. Make it feel like modern CLI (inquirer.js style). |
| - | Terminal color scheme and styling | backlog | P2 | feature | Implement consistent color scheme and styling across all CLI output. **Current state**: Inconsistent or minimal use of colors in terminal output. **Proposed**: Define cohesive color palette for: (1) Status indicators (backlog=gray, in-progress=yellow, done=green, canceled=red). (2) Command output hierarchy (headers=bold, labels=dim, values=normal). (3) Interactive elements (selected=cyan, unselected=white, prompt=blue). (4) Semantic colors (success=green, warning=yellow, error=red, info=blue). (5) Syntax highlighting for code snippets in output. Use library like `chalk` or `picocolors` for terminal colors. Benefits: (a) Better visual hierarchy and scannability. (b) Professional appearance. (c) Matches modern CLI tools (gh, vercel, railway). (d) Accessibility: ensure sufficient contrast, respect NO_COLOR env var. **Design inspiration**: Linear CLI, Vercel CLI, Stripe CLI - clean, minimal, effective use of color. Add `--no-color` flag to disable for CI/scripting. Related to branding (Project 2) - colors should align with logo/brand identity once established. |
| - | Custom color scheme configuration | backlog | P3 | feature | Allow users to customize terminal color scheme via config file. **Depends on**: Terminal color scheme ticket (must have default scheme first). **Implementation**: Add `colors` section to `.prltrc` or `prlt.config.js`: `{ colors: { status: { backlog: 'gray', inProgress: 'yellow', done: 'green' }, semantic: { success: 'green', error: 'red' } } }`. Support: (1) Named colors (red, blue, green, etc.). (2) Hex codes (#FF5733). (3) RGB values (rgb(255, 87, 51)). (4) ANSI 256-color codes. (5) Theme presets (light, dark, solarized, dracula, nord). Commands: `prlt config colors` to view current, `prlt config colors --set <theme>` to apply preset, `prlt config colors --reset` to restore defaults. **Use cases**: (a) Match terminal theme (dark mode users might want different colors). (b) Accessibility (colorblind users can adjust). (c) Personal preference. (d) Team consistency (share config file). **Alternative**: Support terminal theme detection (detect if dark/light mode) and auto-adjust colors. Low priority - nice-to-have polish feature, not critical for MVP. |
| - | Extract copy/strings to separate files for i18n | backlog | P2 | refactor | Refactor all user-facing strings (error messages, prompts, help text, CLI output) into centralized copy files for easier internationalization and content management. **Problem**: Currently strings are hardcoded throughout the codebase in command files, making it difficult to: (1) Translate to other languages (i18n). (2) Maintain consistent voice/tone. (3) Update copy without touching code. (4) Integrate with CMS like Sanity for marketing/product team control. (5) A/B test messaging. **Solution**: Extract strings to structured copy files in `apps/cli/src/copy/` directory. **Proposed structure**: (1) `en.json` (or `en.ts` for type safety) - English strings with keys like `commands.ticket.create.success`, `errors.docker.notFound`, `prompts.agent.selectName`. (2) Organize by domain: `copy/commands/`, `copy/errors/`, `copy/prompts/`. (3) Use i18n library like `i18next` or simple key-value lookup function. (4) Type-safe access: `t('commands.ticket.create.success', { ticketId })` with autocomplete. **Example structure**: `{ "commands": { "ticket": { "create": { "success": "Ticket {{ticketId}} created successfully", "prompt": "Enter ticket description:" } } }, "errors": { "docker": { "notFound": "Docker not found. Install Docker or use --run-on-host flag" } } }`. **Benefits**: (1) **i18n ready**: Easy to add `es.json`, `fr.json`, `ja.json` for international users. (2) **CMS integration**: Sync with Sanity or Contentful for non-eng copy updates. (3) **Consistency**: All copy in one place, easier to review tone/voice. (4) **Versioning**: Track copy changes separately from code. (5) **Testing**: Easier to test different messaging. **Implementation phases**: (1) **Phase 1**: Extract to English-only JSON files, no i18n lib yet. Simple `getCopy(key)` function. (2) **Phase 2**: Add i18n library with locale detection and pluralization. (3) **Phase 3**: Integrate with CMS for marketing team control (Sanity webhook updates copy files). **Related**: This enables future features like locale detection (`prlt config set locale es`), A/B testing copy effectiveness, and accessibility improvements (clearer error messages based on user feedback). **Priority P2**: Not critical for MVP but important for maintainability and future internationalization. Should do before codebase gets too large. **Trade-off**: Adds abstraction layer, slightly more complex to add new strings. But pays off as CLI grows. |
| - | Input validation for prlt commands | backlog | P1 | feature | Add comprehensive input validation across all prlt commands to catch errors early and provide helpful feedback. **Problem**: Currently commands may accept invalid inputs that fail later in execution, or fail with cryptic error messages. Examples: (1) Agent names with invalid characters (spaces, special chars). (2) Ticket IDs that don't exist or have wrong format. (3) Column names that conflict with reserved statuses. (4) Empty or overly long descriptions. (5) Invalid priority levels. (6) Branch names with invalid git characters. (7) Non-existent file paths for specs. **Solution**: Add validation layer before command execution with clear, actionable error messages. **Validation types**: (1) **Format validation** - Agent names: alphanumeric + hyphens/underscores only, max length. Ticket IDs: match TKT-### pattern. Branch names: valid git ref format. (2) **Existence checks** - Agent exists before assigning ticket. Ticket exists before operations. Project exists before creating tickets. (3) **Constraint validation** - Unique names (no duplicate agents, columns). Required fields present (ticket title, description). Value ranges (priority P0-P3). (4) **Business logic** - Can't assign ticket to agent that's already working. Can't move ticket to non-existent column. Can't delete column with tickets. **Implementation approach**: (1) Create validation utilities in `apps/cli/src/lib/validation/`: `validateAgentName(name)`, `validateTicketId(id)`, `validateExists(entity, id)`. (2) Add validation at command entry points before calling storage/business logic. (3) Return descriptive error messages: "Agent name 'my agent' is invalid. Agent names must be alphanumeric with hyphens/underscores only (no spaces)." Instead of: "Error: SQLITE_CONSTRAINT". (4) Use zod or yup for schema validation, or simple custom validators. (5) Add `--skip-validation` flag for power users (use with caution). **Error message format**: "❌ [What's wrong] - [Why it's wrong] - [How to fix it]". Example: "❌ Agent 'my agent' not found - No agent with this name exists - Run 'prlt agent list' to see available agents or 'prlt agent add my-agent' to create one". **Benefits**: (1) Better UX - catch mistakes early with helpful errors. (2) Prevent data corruption - invalid data never reaches database. (3) Clearer contracts - users understand what inputs are valid. (4) Easier debugging - validation errors are distinct from runtime errors. (5) Documentation - validation rules serve as implicit API docs. **Priority P1**: Critical for production readiness. Poor validation leads to confusion, support burden, and potential bugs. Should implement before beta launch to establish good patterns. **Related to**: Better error messages (Project 1), extract copy for i18n (error messages should be in copy files). |
| - | Terminal style marketplace | backlog | P2 | feature | Community marketplace for sharing and discovering custom terminal styles/themes for prlt CLI output. **Vision**: Let users share complete visual styles (color schemes, formatting, icons, layout templates) that transform the look and feel of prlt's terminal output. **Difference from agent themes**: Agent themes = agent names/personalities. Style marketplace = CLI visual appearance (colors, formatting, output structure). **Commands**: (1) `prlt style browse` - Browse available community styles from registry. Preview screenshots/examples. Filter by category (minimal, retro, cyberpunk, corporate, etc.). (2) `prlt style install <style-name>` - Install style (e.g., `prlt style install cyberpunk-neon`). Downloads to `~/.proletariat/styles/`. (3) `prlt style apply <style-name>` - Apply installed style as active theme. Updates config. (4) `prlt style preview <style-name>` - Preview style with sample output before installing. (5) `prlt style publish` - Publish custom style to marketplace. (6) `prlt style create <name>` - Scaffold new style template with default values. (7) `prlt style list --local` - Show locally installed styles. (8) `prlt style reset` - Reset to default prlt style. **Style definition structure**: JSON/YAML defining complete visual theme: `{ "name": "cyberpunk-neon", "version": "1.0.0", "author": "username", "description": "Neon colors and retro-futuristic ASCII art", "colors": { "status": { "backlog": "#FF00FF", "inProgress": "#00FFFF", "done": "#00FF00" }, "semantic": { "success": "#39FF14", "error": "#FF073A" } }, "formatting": { "borders": "double", "icons": "nerd-fonts", "headers": "ascii-art" }, "layout": { "table": "compact", "spacing": "tight" } }`. **Style components**: (1) **Color palette** - All semantic colors, status colors, syntax highlighting. (2) **Typography** - Bold/dim/italic usage, font effects. (3) **Borders & boxes** - Box-drawing characters (single, double, rounded, ASCII). (4) **Icons** - Emoji, nerd fonts, ASCII symbols, or none. (5) **Layout density** - Compact vs spacious, table formats. (6) **ASCII art** - Custom headers, logos, dividers. (7) **Output format** - Table vs list vs tree views. **Marketplace implementation**: (1) **Phase 1**: Manual sharing via GitHub repos with `.prlt-style.json` files. (2) **Phase 2**: CLI commands to install from GitHub URLs. (3) **Phase 3**: Dedicated style registry with web gallery showing screenshots. **Preview system**: Generate sample output (ticket list, agent list, board view) with applied style so users can see before installing. Could render to image or display in terminal with `prlt style preview`. **Popular style examples**: (1) **Minimal**: Black/white, no colors, simple tables. (2) **Solarized**: Solarized dark/light color schemes. (3) **Dracula**: Popular Dracula theme colors. (4) **Retro terminal**: Green monochrome, CRT effects. (5) **Corporate**: Professional, muted colors, formal layout. (6) **Hacker**: Matrix green, dense info, minimal spacing. (7) **Pastel**: Soft colors, rounded corners, friendly. **Monetization potential**: (1) Premium styles with revenue split. (2) Designer partnerships (featured creators). (3) Team licensing (unified company styles). **Related to**: Terminal color scheme (line 116 - base implementation), custom color config (line 117 - local customization), agent theme marketplace (line 102 - similar marketplace pattern). **Benefits**: (1) **Personalization**: Users express individuality through CLI appearance. (2) **Accessibility**: High-contrast styles for visibility, colorblind-friendly palettes. (3) **Brand alignment**: Teams can match company colors. (4) **Community creativity**: Users build/share creative designs. (5) **Differentiation**: No other CLI tool has style marketplace at this level. (6) **Engagement**: Style customization drives usage and social sharing. **Examples of inspiration**: Oh My Zsh themes, Starship prompts, iTerm2 color schemes, VS Code themes, Neovim colorschemes. **Priority P2**: Fun differentiator but requires base styling system first (line 116). Great post-launch community feature once core functionality is solid. Could drive viral growth through social media (users sharing cool terminal screenshots). |
| - | Progress indicators for long operations | backlog | P3 | feature | Show progress for operations like container builds, batch spawns. |
| - | React Ink migration | backlog | P2 | refactor | Explore migrating CLI output and prompts from chalk/inquirer to React Ink for unified component-based UI. **Current state**: Mixed use of chalk for styling, console.log for output, and inquirer for prompts. Inconsistent patterns across commands. **Proposed**: Adopt React Ink as rendering layer for all CLI output. **Benefits**: (1) **Reusable components** - Build `<TicketList>`, `<AgentStatus>`, `<BoardView>`, `<ProgressBar>` components used across commands. (2) **Consistent styling** - Single source of truth for colors, spacing, borders via Ink's `<Box>`, `<Text>`. (3) **Interactive UIs** - Live updates, keyboard navigation, spinners without third-party libs. (4) **Replace inquirer** - Use `@inkjs/ui` components (`<SelectInput>`, `<TextInput>`, `<ConfirmInput>`) instead of inquirer prompts. Eliminates paradigm switching between React render and blocking prompts. (5) **Testability** - Snapshot test CLI output like React components. (6) **State management** - React hooks for complex interactive flows. **Migration phases**: (1) **Phase 1 - Exploration**: Build POC with one command (e.g., `ticket list`) using Ink. Compare DX and output quality. (2) **Phase 2 - Component library**: Create shared Ink components in `apps/cli/src/components/`: `<Table>`, `<StatusBadge>`, `<Spinner>`, `<SelectPrompt>`. (3) **Phase 3 - Display commands**: Migrate read-only commands (list, show, status) to Ink. Keep inquirer for prompts initially. (4) **Phase 4 - Interactive commands**: Replace inquirer prompts with Ink equivalents in create/edit commands. (5) **Phase 5 - Full migration**: Remove chalk/inquirer deps, all output through Ink. **Trade-offs**: (1) Additional dependency (ink + react). (2) Learning curve for team. (3) May be overkill for simple output - evaluate in Phase 1. **oclif compatibility**: oclif doesn't require inquirer - just provides command structure. Ink works fine inside `run()` method, just stop using `this.log()`. **Related to**: Terminal color scheme (line 122), style marketplace (line 126), progress indicators (line 127) - Ink would make all these easier to implement. **Priority P2**: Good DX improvement but not blocking MVP. Evaluate after core functionality stable. |

---

## Project 8: Additional Integrations (Future)

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| - | Jira integration | future | P2 | feature | Enterprise integration. Commands: `prlt jira connect`, `sync`, `import`, `export`. OAuth/API key setup, bidirectional sync. Map tickets ↔ issues, statuses ↔ workflow states. Required for enterprise adoption - Jira used for compliance/reporting. Atlassian ecosystem (~10M users). |
| - | Shortcut integration | future | P3 | feature | Sync with Shortcut (formerly Clubhouse). Modern PM tool popular with product teams. API similar to Linear. Commands: `prlt shortcut connect/sync/import/export`. |
| - | ClickUp integration | future | P3 | feature | All-in-one tool with growing user base. Complex API (tasks, docs, goals, etc.). Start with task sync only. Commands: `prlt clickup connect/sync`. |
| - | Asana integration | future | P3 | feature | Widespread adoption in non-tech teams. Task/project sync. Important for product teams and cross-functional orgs. Commands: `prlt asana connect/sync`. |
| - | Monday.com integration | future | P3 | feature | Enterprise work OS platform. Popular in non-tech orgs. Board/item sync. Commands: `prlt monday connect/sync`. |
| - | Notion integration | future | P3 | feature | Sync with Notion databases. Import/export. Community-requested feature. Knowledge worker favorite. |
| - | GitHub Projects integration | future | P3 | feature | Sync with GitHub Projects boards (v2). Different from GitHub Issues - this is the project board layer. Lower priority since GitHub Issues covers main use case. |

**Note**: Community can build additional integrations via plugin system (Project 3). Focus internal dev on high-impact integrations (GitHub/Linear/GitLab/Jira). Let ecosystem handle long tail (Basecamp, Trello, Height, Plane, etc.).

---

## Project 9: Cloud Mode & Team Collaboration (Future)

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| - | Cloud Mode architecture design | future | P2 | feature | Design architecture for Cloud Mode: Docker-based agents running in cloud with web dashboard. **Key decisions**: (1) Hosting model: User-hosted (Docker Compose/K8s) vs managed SaaS vs hybrid. (2) Database: Migrate from SQLite to Postgres for multi-user support. (3) Agent execution: Remote Docker hosts, Kubernetes pods, or serverless containers. (4) Authentication: Multi-user auth, team workspaces, SSO. (5) API layer: REST/GraphQL API for web dashboard. (6) Real-time updates: WebSockets for live agent status. (7) Storage: Cloud storage for agent outputs/logs (S3, GCS). (8) Pricing model: Free tier, usage-based, seat-based. This is a major architectural shift from local CLI to cloud platform. Requires backend infrastructure, web frontend, deployment pipeline. Should validate local CLI adoption first before investing in Cloud Mode. |
| - | Multi-user collaboration (local) | future | P2 | feature | Enable multiple developers to share a local prlt workspace via git. **Current limitation**: SQLite database (workspace.db) in .proletariat/ creates merge conflicts when multiple devs work in same repo. **Proposed solution**: (1) Make PMO data git-friendly - store tickets/projects as YAML/JSON files in pmo/ directory instead of SQLite, or use SQLite with git-friendly schema (append-only, UUIDs, conflict resolution). (2) Add `prlt sync` command to merge changes from teammates. (3) Conflict resolution UI for ticket updates. (4) Lock mechanism for active work (can't claim ticket already claimed by teammate). **Alternative**: Skip local multi-user, push users toward Cloud Mode for team usage. Or recommend GitHub Issues/Linear integration for team collaboration (prlt stays single-user, syncs with shared PM tool). |
| - | Database sync/replication | future | P3 | feature | Sync local SQLite database with remote backup or team database. CRDTs, operational transforms, or simple last-write-wins. Enables: (1) Backup to cloud storage. (2) Multi-device usage (laptop + desktop share same workspace). (3) Team collaboration without Cloud Mode. **Challenges**: SQLite not designed for replication, conflict resolution is hard, network partition handling. **Alternative**: Use Litestream for SQLite replication to S3/GCS (simpler, backup-focused), or migrate to Postgres with logical replication. |
| - | Web dashboard for Cloud Mode | future | P3 | feature | Web UI for Cloud Mode. Features: (1) View tickets/boards (Kanban, list, calendar views). (2) Create/edit tickets. (3) Monitor agent status (live updates via WebSockets). (4) View agent logs/outputs. (5) Review PRs created by agents. (6) Team management (invite users, assign tickets). (7) Analytics dashboard (velocity, completion rates). Tech stack: React/Next.js frontend, connects to prlt API. Complements CLI - not a replacement. Power users still use CLI, managers/PMs use web dashboard. |
| - | Remote agent execution API | future | P3 | feature | API to spawn agents on remote infrastructure instead of local Docker. Commands: `prlt work spawn --remote` or `prlt config set execution-mode remote`. Agent runs in cloud (K8s pod, ECS task, Lambda), outputs stream back to CLI. Requires Cloud Mode backend. Enables: (1) Run expensive work on beefy cloud machines. (2) Keep laptop closed, agents work overnight. (3) Team shares agent pool. |

---

## Project 10: Infrastructure (Defer)

| Ticket | Title | Status | Priority | Category | Description |
|--------|-------|--------|----------|----------|-------------|
| - | Database abstraction class (db.ts) | backlog | P1 | refactor | Create a centralized database abstraction class to standardize connection management, query execution, and error handling across the codebase. **Problem**: Database access is scattered - `storage-sqlite.ts` has its own patterns, `database/index.ts` has different patterns, commands open/close connections inconsistently, and there's no unified error handling or connection pooling. **Proposed solution**: Create `apps/cli/src/lib/db.ts` with a `Database` class that wraps better-sqlite3 and provides: (1) **Singleton connection management** - Single connection per workspace, lazy initialization, automatic cleanup. No more `db.close()` scattered everywhere. (2) **Standardized query methods** - `db.query<T>(sql, params)`, `db.exec(sql)`, `db.transaction(fn)` with consistent return types. (3) **Error handling** - Wrap SQLite errors with helpful messages ("Column X not found" → "Database schema outdated, run prlt migrate"). (4) **Migration support** - `db.migrate()` runs pending migrations, `db.schemaVersion` returns current version. (5) **Type safety** - Generic query methods that return typed results: `db.query<Ticket>('SELECT * FROM tickets')`. (6) **Logging/debugging** - Optional query logging for debugging: `PRLT_DB_DEBUG=1 prlt ticket list`. (7) **Connection events** - Hooks for connection open/close/error for future telemetry. **Usage pattern**: ```typescript import { db } from '../lib/db.js'; // Anywhere in codebase const tickets = db.query<Ticket>('SELECT * FROM pmo_tickets WHERE status = ?', ['backlog']); // vs current scattered pattern const dbPath = getDatabasePath(workspacePath); const database = new Database(dbPath); const tickets = database.prepare('SELECT...').all(); database.close(); ``` **Benefits**: (1) **Consistency** - One way to access database everywhere. (2) **Fewer bugs** - No forgotten `db.close()`, no connection leaks. (3) **Easier testing** - Mock single `db` module instead of better-sqlite3 everywhere. (4) **Future flexibility** - Swap SQLite for Postgres by changing one file (for cloud mode). (5) **Better errors** - Centralized error handling with helpful messages. **Implementation**: (1) Create `db.ts` with `Database` class. (2) Export singleton instance. (3) Gradually migrate existing code to use new class. (4) Deprecate direct better-sqlite3 usage. **Related**: ORM evaluation (line below), storage-sqlite refactor (line below) - this is lighter weight than ORM, good stepping stone. **Priority P1**: Reduces bugs, improves DX, and is prerequisite for cleaner architecture. |
| - | ORM layer for database abstraction | backlog | P2 | refactor | Evaluate adding an ORM (Drizzle, Kysely, TypeORM, or Prisma) to replace raw SQL queries in storage-sqlite.ts. **Current state**: Direct better-sqlite3 usage with hand-written SQL, prepared statements, manual schema management. **Problem**: Schema changes require manual SQL + migration scripts, no type safety between DB and TypeScript, verbose query code, risk of SQL injection if not careful. **Benefits of ORM**: (1) Type-safe queries - schema defined once, types auto-generated. (2) Easier migrations - schema changes = code changes, migrations auto-generated. (3) Cleaner query code - builder pattern vs string interpolation. (4) Future multi-DB support - swap SQLite for Postgres/MySQL without rewriting queries. **Tradeoffs**: (1) Added dependency + complexity. (2) Learning curve. (3) Performance overhead (usually negligible for CLI tool). (4) Lock-in to ORM's query patterns. **Recommendation**: Drizzle or Kysely (lightweight, TypeScript-first, minimal magic) over TypeORM/Prisma (heavier, more abstraction). Evaluate whether benefits justify refactor effort - current SQL approach works fine for MVP. Could defer until adding Postgres support for Cloud Mode. If implementing, do AFTER refactoring storage-sqlite.ts into modules (easier to swap out module internals than monolith). **Alternative**: Keep raw SQL but add code generation for types from schema (like sqlc for Go). |
| - | Strict TypeScript types across codebase | backlog | P1 | refactor | Add comprehensive TypeScript types to the entire codebase, eliminating `any` types and enabling strict mode. **Current state**: Mixed type coverage - some files well-typed, others use `any`, implicit any in function params, loose object typing, database rows cast with `as` without validation. **Problem**: (1) Runtime errors that TypeScript should catch at compile time. (2) IDE autocomplete doesn't work in untyped areas. (3) Refactoring is risky - no compiler to catch mistakes. (4) New contributors can't understand function contracts. **Solution - Phased approach**: **Phase 1 - Enable strict mode**: (1) Add `"strict": true` to tsconfig.json (or enable individual flags: `noImplicitAny`, `strictNullChecks`, `strictFunctionTypes`). (2) Fix resulting errors file by file. (3) Track progress: `tsc --noEmit | grep error | wc -l`. **Phase 2 - Core types** (`apps/cli/src/lib/types.ts`): (1) **Entity types**: `Ticket`, `Agent`, `Project`, `Spec`, `Epic`, `Column`, `Subtask` - full interface definitions matching DB schema. (2) **Config types**: `WorkspaceConfig`, `HQConfig`, `PMOConfig`, `ThemeConfig`. (3) **Command types**: `CommandFlags`, `CommandArgs` for each command. (4) **API response types**: GitHub API responses, any external API calls. (5) **Event types**: For hooks, notifications, pub/sub if used. **Phase 3 - Database typing**: (1) Type all DB query results - no more `as Ticket` without validation. (2) Use generics: `db.query<Ticket>(sql)` returns `Ticket[]`. (3) Consider zod or io-ts for runtime validation of DB rows. (4) Generate types from schema (like Prisma/Drizzle do). **Phase 4 - Function signatures**: (1) All exported functions have explicit return types. (2) All function parameters typed (no implicit any). (3) Use discriminated unions for command results: `{ success: true, data: T } | { success: false, error: string }`. **Phase 5 - Eliminate escape hatches**: (1) Search and fix all `any` types: `grep -r ": any" src/`. (2) Search and fix all `as` casts without validation. (3) Search and fix all `// @ts-ignore` comments. (4) Add eslint rules: `@typescript-eslint/no-explicit-any`, `@typescript-eslint/no-unsafe-*`. **Key files to type**: (1) `storage-sqlite.ts` - All 50+ methods need typed returns. (2) `database/index.ts` - Core DB operations. (3) Command files - Flags, args, run() returns. (4) `themes.ts` - Theme configuration. (5) Utility functions throughout lib/. **Tooling**: (1) Add `@typescript-eslint/recommended-type-checked` eslint config. (2) Add `tsc --noEmit` to pre-commit hook. (3) Add type coverage tool: `type-coverage` or `typescript-coverage-report`. (4) Target: 95%+ type coverage before launch. **Benefits**: (1) **Catch bugs at compile time** - No more "undefined is not a function". (2) **Better IDE experience** - Autocomplete, go-to-definition, refactoring. (3) **Self-documenting code** - Types are documentation. (4) **Safer refactoring** - Compiler catches breaking changes. (5) **Onboarding** - New contributors understand codebase faster. **Priority P1**: Technical debt that compounds. Every new file written without types makes this harder. Do before codebase grows more. Foundation for other refactors (DB abstraction, storage modules). |
| - | Refactor storage-sqlite.ts into separate modules | backlog | P2 | refactor | Break up the monolithic 1891-line `apps/cli/src/lib/pmo/storage-sqlite.ts` file into focused modules for maintainability. Current file has ~50+ methods covering projects, tickets, columns, specs, epics, subtasks, sync, and utilities. **Proposed structure**: Split into separate files in `apps/cli/src/lib/pmo/storage/`: (1) `base.ts` - SQLiteStorage class, constructor, DB connection, schema initialization, migrations. (2) `projects.ts` - createProject, getProject, listProjects, deleteProject, setCurrentProject. (3) `columns.ts` - createColumn, renameColumn, moveColumn, deleteColumn. (4) `tickets.ts` - createTicket, getTicket, updateTicket, moveTicket, deleteTicket, listTickets. (5) `subtasks.ts` - addSubtask, toggleSubtask, removeSubtask. (6) `specs.ts` - createSpec, getSpec, updateSpec, deleteSpec, listSpecs, linkTicketToSpec, spec dependencies. (7) `epics.ts` - createEpic, getEpic, updateEpic, deleteEpic, listEpics, reorderEpic, linkTicketToEpic. (8) `sync.ts` - pull, push, status methods (currently stubs). (9) `utils.ts` - Private helper methods like rowToTicket, getMaxPosition, updateTimestamp. Use TypeScript mixins or composition pattern to combine modules back into single SQLiteStorage export. Alternatively, keep single class but organize methods with clear section comments and consider extracting complex logic into pure functions. This is a refactor, not a rewrite - preserve all existing behavior and tests. **Benefits**: Easier navigation, clearer ownership of feature areas, simpler testing, reduced merge conflicts. **Risk**: Medium - need to carefully preserve method interdependencies and private state access. Test coverage critical before refactoring. |
| - | Remove old-cli from repo | deferred | P3 | chore | Clean up legacy `old-cli/` directory from repository. Archive or delete deprecated code. Only remove once confirmed nothing is needed from old implementation. Low priority - doesn't affect functionality, just repo cleanliness. |
| - | DB schema migrations | deferred | P3 | chore | Versioned migrations (up/down). `prlt migrate` to run pending. Handle schema evolution. |
| - | Upgrade command | deferred | P3 | chore | `prlt upgrade` - runs pending migrations, handles version bumps, any data transforms needed between versions. User-friendly wrapper around migrations. |
| - | Auto-deploy to npm | deferred | P3 | chore | Auto-deploy on release. Versioning strategy (semver). |
| - | Homebrew distribution | backlog | P1 | distribution | Publish prlt to Homebrew for macOS/Linux installation via `brew install proletariat`. **Why critical for adoption**: (1) **Frictionless install**: `brew install` is muscle memory for developers - one command vs multi-step npm global install. (2) **Discoverability**: Homebrew users search `brew search <tool>` to find CLI tools. Being in Homebrew = legitimacy signal. (3) **Automatic updates**: `brew upgrade` keeps users on latest version. (4) **System integration**: Homebrew handles PATH, dependencies, permissions automatically. No global npm conflicts. (5) **Cross-platform**: Works on macOS and Linux (Linuxbrew/Homebrew on Linux). **Implementation**: (1) **Create Homebrew formula**: Ruby DSL defining: package name, version, download URL (GitHub release tarball), install script, dependencies. Example structure: `class Proletariat < Formula; desc "Multi-agent development orchestration"; homepage "https://github.com/..."; url "https://github.com/.../archive/v1.0.0.tar.gz"; sha256 "..."; depends_on "node"; def install; system "npm", "install", "--production"; bin.install_symlink Dir["node_modules/.bin/*"]; end; end`. (2) **Publish to Homebrew core** (official tap): Submit PR to homebrew/homebrew-core repo. Requires: (a) Notable project (500+ stars or significant adoption). (b) Stable versioning (semver, tagged releases). (c) Automated testing (formula must build successfully). High bar but worth it for maximum distribution. (3) **Alternative - Custom tap** (easier first step): Create `homebrew-proletariat` repo with formula. Users install via `brew tap your-org/proletariat && brew install proletariat`. Lower friction to start, migrate to core tap later. **GitHub release automation**: (1) On version tag (e.g., `v1.0.0`), GitHub Actions builds tarball and creates release. (2) Formula automatically updates download URL and SHA256 hash. (3) Use `brew bump-formula-pr` command to auto-create PR to Homebrew. **Testing formula locally**: `brew install --build-from-source ./proletariat.rb` to test before publishing. **Benefits by the numbers**: (a) Homebrew has ~1M active users, many searching for dev tools. (b) Developer surveys show 70%+ macOS devs use Homebrew as primary package manager. (c) Projects see 2-5x increase in installs after Homebrew listing (anecdotal: gh CLI, vercel, railway). **Related patterns**: `gh` (GitHub CLI), `vercel`, `railway`, `stripe` all publish to Homebrew. It's table stakes for CLI tool distribution. **Phase 1 (P1 - pre-launch)**: Custom tap with formula. Users can `brew install`. **Phase 2 (post-launch, 1k+ stars)**: Submit to homebrew-core for official listing. **Success metrics**: Track installs via Homebrew analytics (opt-in). Compare npm vs brew install ratios. **Priority P1**: Essential for macOS adoption - most devs prefer `brew install` over `npm install -g`. Should have before public launch to reduce install friction. Build Homebrew formula in parallel with Linux support (line 29). **Related to**: Linux/Windows support (line 29 - Homebrew works on Linux too), auto-deploy to npm (line 164 - complementary distribution channel). **Commands to document**: Update README with `brew install proletariat` as primary install method on macOS, `npm install -g` as fallback. |
| - | Package distribution (APT, YUM, AUR, Chocolatey, Scoop) | backlog | P1 | distribution | Distribute prlt through native package managers for Linux and Windows to maximize adoption and reduce install friction. **Problem**: `npm install -g` is unfamiliar to non-JS developers. Native package managers (APT, YUM, Chocolatey) are preferred by system admins, DevOps engineers, and infrastructure teams - exactly the audience for multi-agent orchestration tools. **Linux package managers**: (1) **APT (Debian/Ubuntu)** - 40% of Linux desktop users, dominant in cloud/servers. Create `.deb` package, host in PPA (Personal Package Archive) or packagecloud.io. Install: `sudo add-apt-repository ppa:yourorg/proletariat && sudo apt update && sudo apt install proletariat`. (2) **YUM/DNF (RHEL/Fedora/CentOS)** - 25% of Linux servers (enterprise heavy). Create `.rpm` package, host in YUM repository. Install: `sudo yum install proletariat`. (3) **AUR (Arch User Repository)** - Popular with power users/enthusiasts. Community-maintained, high engagement. Create PKGBUILD script. Install: `yay -S proletariat`. (4) **Snap (Universal)** - Cross-distro, Ubuntu default. Create snapcraft.yaml. Install: `sudo snap install proletariat`. (5) **Flatpak (Universal)** - Alternative to Snap, growing adoption. **Windows package managers**: (1) **Chocolatey** - Most popular Windows package manager (100M+ downloads). Create `.nuspec` file defining package metadata. Install: `choco install proletariat`. Used heavily in enterprise Windows environments. (2) **Scoop** - Modern Windows package manager, popular with developers. Simpler than Chocolatey, git-based. Create manifest JSON. Install: `scoop install proletariat`. Preferred by WSL2 users. (3) **Winget (Microsoft)** - Official Windows Package Manager, ships with Windows 11. Create manifest YAML. Install: `winget install proletariat`. Growing rapidly, will become default. **Implementation strategy**: (1) **Packaging tools**: Use `fpm` (Effing Package Management) to build `.deb`, `.rpm` from single config. Use `electron-builder` or `pkg` to bundle Node.js binary with prlt (avoid Node.js dependency for native packages). (2) **Hosting options**: (a) packagecloud.io - Hosts APT/YUM repos, free for open source. (b) GitHub Releases - Host `.deb`/`.rpm` files directly, users download manually. (c) Official repos - Submit to distro repos (Debian sid, Fedora COPR, AUR) for maximum trust. (3) **Automated builds**: GitHub Actions workflow builds all package formats on release. Matrix: `[deb, rpm, snap, nupkg]`. Upload to package repositories via API. **Bundled vs Node.js dependency**: (1) **Option A - Bundled binary**: Use `pkg` or `nexe` to create standalone binary with Node.js runtime included. Pros: No Node.js install required, smaller attack surface. Cons: Larger package size (~50MB), harder to update Node.js version. (2) **Option B - Node.js dependency**: Packages declare `nodejs >= 18` as dependency. Package manager auto-installs Node.js if needed. Pros: Smaller package, easier to update. Cons: Users might already have Node via nvm/different version, dependency hell. **Recommendation**: Start with bundled binary for simplicity - avoid Node.js version conflicts, better UX ("just works"). **Distribution timeline**: (1) **Phase 1 (MVP - P1)**: Homebrew (macOS/Linux), Chocolatey (Windows), GitHub releases with `.deb`/`.rpm` for manual install. Cover 80% of users. (2) **Phase 2 (Post-launch)**: Submit to official repos (homebrew-core, Debian sid, Fedora COPR, AUR). (3) **Phase 3 (Scale)**: Snap, Flatpak, Winget, Scoop for niche users. **Why P1 priority**: (1) **Audience mismatch**: Backend/DevOps engineers (target audience for agent orchestration) prefer native packages over npm. npm is for JS devs. (2) **Trust signal**: Being in distro repos = vetted, secure, maintained. Increases perceived legitimacy. (3) **Enterprise adoption**: IT departments whitelist package sources. Chocolatey/APT approval is easier than "install random npm package globally". (4) **Competitive parity**: All major CLI tools (docker, kubectl, terraform, gh) ship via native packages. Not having them is a red flag. **Success metrics**: Track install sources via telemetry (opt-in). Goal: 50%+ installs via native packages (not npm) within 6 months of launch. **Related to**: Linux/Windows support (line 29 - prerequisite), Homebrew distribution (line 165 - part of broader distribution strategy), auto-deploy to npm (line 164 - complementary, not replacement). **Documentation updates**: README should list install methods by platform: macOS (`brew install`), Ubuntu (`apt install`), RHEL (`yum install`), Windows (`choco install`), fallback (`npm install -g`). |
| - | GitHub Releases with pre-built binaries | backlog | P1 | distribution | Publish pre-built binaries for all platforms (macOS, Linux, Windows) to GitHub Releases on every version tag. **Purpose**: Provide zero-dependency download option for users who don't use package managers or npm. Especially important for: (1) Enterprise environments with restricted package manager access. (2) Users who want specific version (not latest). (3) Quick evaluation without install (download, run, delete). (4) CI/CD environments that prefer direct binary downloads. **Binaries to build**: (1) **macOS**: `prlt-darwin-x64` (Intel), `prlt-darwin-arm64` (Apple Silicon), universal binary `prlt-darwin-universal`. (2) **Linux**: `prlt-linux-x64` (most common), `prlt-linux-arm64` (Raspberry Pi, ARM servers), `prlt-linux-armv7` (32-bit ARM). (3) **Windows**: `prlt-win-x64.exe`, `prlt-win-arm64.exe`. **Binary creation**: Use `pkg` (Vercel's Node.js packager) or `nexe` to bundle Node.js runtime + prlt code into single executable. Example: `pkg . --targets node18-macos-x64,node18-linux-x64,node18-win-x64 --output dist/prlt`. Results in ~40-50MB binaries (includes Node.js). **GitHub Actions workflow**: On tag push (`v*`): (1) Build all binary variants in parallel (matrix strategy). (2) Create GitHub Release with changelog (auto-generated from commits). (3) Upload binaries as release assets. (4) Generate SHA256 checksums for verification. (5) Create archive files (`.tar.gz` for Unix, `.zip` for Windows). **Release asset structure**: ``` v1.0.0/ ├── prlt-darwin-x64 ├── prlt-darwin-arm64 ├── prlt-linux-x64 ├── prlt-linux-arm64 ├── prlt-win-x64.exe ├── checksums.txt └── CHANGELOG.md ``` **Install script**: Create install script for Unix systems: `curl -fsSL https://raw.githubusercontent.com/org/prlt/main/install.sh | sh`. Script detects OS/arch, downloads correct binary, installs to `/usr/local/bin`, sets executable permission. Windows: PowerShell script equivalent. **Auto-update mechanism**: Binary can check GitHub Releases API for newer version, prompt user to upgrade. `prlt upgrade` command downloads latest binary and replaces itself. **Comparison to other tools**: (1) **deno**: Ships pre-built binaries via GitHub Releases, `curl | sh` install script. (2) **gh CLI**: Pre-built binaries + package managers (Homebrew, APT, etc.). (3) **docker-compose**: Pre-built binaries on GitHub Releases as primary distribution. This is standard practice for Go/Rust CLIs that compile to native binaries. Node.js tools less common but growing (Vercel, Prisma do this). **Benefits**: (1) **No runtime dependencies**: Works even if Node.js not installed. (2) **Hermetic**: Exact version control, no dependency conflicts. (3) **Fast download**: Direct binary download faster than npm install with dependencies. (4) **Offline install**: Download once, install on air-gapped systems. (5) **Trustworthy**: Users can verify SHA256 checksums, inspect single binary vs npm dependency tree. **Tradeoffs**: (1) **Large file size**: 40-50MB per binary vs ~1MB npm package (but users have to install Node.js separately). (2) **Build complexity**: Requires cross-compilation setup, testing on all platforms. (3) **Update mechanism**: Can't use `npm update -g`, need custom updater. **Phase 1 (P1 - pre-launch)**: Build macOS and Linux x64 binaries, publish to GitHub Releases. Add install script. **Phase 2 (post-launch)**: Add ARM variants, Windows binaries. **Phase 3**: Auto-update mechanism in binary. **Priority P1**: Essential for enterprise adoption and reducing install friction. Many users prefer `curl | sh` over package managers. Combined with Homebrew/Chocolatey (lines 165-166), covers all distribution preferences. **Related to**: Auto-deploy to npm (line 164 - part of automated release pipeline), Homebrew distribution (line 165 - Homebrew formula points to GitHub Release tarball), package distribution (line 166 - native packages also use GitHub Releases as source). **Success metrics**: Track downloads by platform/arch from GitHub Releases. Compare to package manager installs. |
| - | E2E test harness and selective test execution | backlog | P1 | test | Build comprehensive e2e test harness with ability to run all tests or selective test suites. **Problem**: Currently no structured way to run e2e tests selectively - either run all or manually specify individual test files. Need organized test harness for efficient development and CI. **Solution**: Implement test organization and runner commands. **Test organization**: (1) **Test suites by feature area**: `test/e2e/agents/`, `test/e2e/tickets/`, `test/e2e/work/`, `test/e2e/board/`, `test/e2e/docker/`, `test/e2e/git/`. (2) **Test tags/markers**: Tag tests with categories (@smoke, @critical, @integration, @slow, @docker-required). (3) **Test fixtures**: Shared setup/teardown for common scenarios (init workspace, create agents, seed tickets). **Commands to add**: (1) `pnpm test` - Run all tests (unit + e2e). (2) `pnpm test:e2e` - Run all e2e tests. (3) `pnpm test:e2e:agents` - Run only agent-related e2e tests. (4) `pnpm test:e2e:tickets` - Run only ticket-related e2e tests. (5) `pnpm test:e2e:smoke` - Run smoke tests (fast, critical paths). (6) `pnpm test:e2e:critical` - Run critical path tests. (7) `pnpm test:e2e:watch` - Watch mode for development. (8) `pnpm test:e2e:debug` - Run with debugger attached. (9) `pnpm test:e2e --grep "pattern"` - Run tests matching pattern. **Test categories**: (1) **Smoke tests** (< 30s total): Init workspace, create agent, create ticket, assign ticket, basic list commands. Critical user journeys. (2) **Critical tests** (< 2min total): Full workflows end-to-end (create ticket → spawn work → create PR). (3) **Integration tests** (< 5min total): Multi-component interactions, database operations, git operations. (4) **Docker tests** (variable time): Tests requiring Docker (spawn work in container, devcontainer tests). Skip if Docker unavailable. **Test framework enhancements**: (1) **Parallel execution**: Run independent test suites in parallel (Jest/Vitest built-in). (2) **Test isolation**: Each test gets isolated temp directory and database (`PRLT_HOME=/tmp/test-$$`). (3) **Cleanup automation**: Auto-cleanup temp dirs, Docker containers, git worktrees after tests. (4) **Retry logic**: Retry flaky tests (Docker pulls, network operations) before failing. (5) **Test reporting**: JUnit XML output for CI, coverage reports, performance tracking. **Package.json scripts structure**: ```json { "test": "pnpm test:unit && pnpm test:e2e", "test:unit": "vitest run src/**/*.test.ts", "test:e2e": "vitest run test/e2e/**/*.test.ts", "test:e2e:agents": "vitest run test/e2e/agents/**/*.test.ts", "test:e2e:tickets": "vitest run test/e2e/tickets/**/*.test.ts", "test:e2e:work": "vitest run test/e2e/work/**/*.test.ts", "test:e2e:smoke": "vitest run test/e2e/**/*.test.ts -t smoke", "test:e2e:critical": "vitest run test/e2e/**/*.test.ts -t critical", "test:e2e:watch": "vitest watch test/e2e/**/*.test.ts", "test:e2e:debug": "node --inspect-brk ./node_modules/.bin/vitest run test/e2e/**/*.test.ts" } ``` **CI integration**: (1) **PR checks**: Run smoke + critical tests on every PR (~2min). (2) **Merge to main**: Run full e2e suite (~5min). (3) **Nightly**: Run all tests including slow/docker tests (~15min). (4) **Skip Docker tests in CI if needed**: Use `--skipDockerTests` flag or detect Docker availability. **Benefits**: (1) **Faster feedback loop**: Run relevant tests only during development. (2) **Efficient CI**: Run fast smoke tests on PR, full suite on merge. (3) **Better organization**: Clear test structure, easy to find/add tests. (4) **Developer experience**: Watch mode, debugging support, isolated tests. (5) **Confidence**: Comprehensive test coverage with selective execution. **Priority P1**: Critical for maintaining quality as codebase grows. Without selective test execution, developers wait too long for feedback or skip tests entirely. Essential before launch to establish good testing patterns. **Related to**: Automated PR checks (line 152 - consumes this harness), local dev testing workflow (line 7 - uses PRLT_HOME isolation). |
| - | Automated PR checks (e2e, static analysis, build) | backlog | P1 | chore | Set up GitHub Actions workflow to automatically run quality checks on all PRs for feature branches. **Required checks**: (1) **E2E tests** - Run full end-to-end test suite (`pnpm test` or `pnpm test:e2e`). (2) **Static analysis** - Run TypeScript type checking (`tsc --noEmit`), ESLint (`pnpm lint`), and any other static analysis tools. (3) **Build verification** - Run `pnpm build` to ensure build succeeds. (4) **PR blocking** - Configure branch protection rules to require all checks to pass before merging. **Workflow trigger**: On PR open/update for all branches except main. **Benefits**: Catch regressions before merge, enforce code quality standards, prevent broken builds from landing. **Config location**: `.github/workflows/pr-checks.yml`. Priority P1 since this is critical for maintaining code quality as we approach launch. |
| - | GitHub Actions CI/CD | deferred | P3 | chore | Build/test/publish pipeline. |
| - | Command deprecation with aliases | deferred | P3 | chore | Handle command renames gracefully. Old names become aliases with deprecation warnings. |
