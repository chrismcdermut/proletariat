# NPM configuration for private package
# Configure registry for scoped packages
@chrismcdermut:registry=https://registry.npmjs.org/

# Ensure authentication is required for private packages
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Package access configuration
access=restricted

# Always save exact versions
save-exact=true

# Package lock file
package-lock=false